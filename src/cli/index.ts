// CLI entry: `build:m1` runs the pipeline (script.yaml -> Story IR -> Scene IR, Zod-validated)
// then renders via Remotion renderMedia -> out.mp4. Single command, no manual steps in the loop.
// See spec §15 (M1 acceptance #1). Placeholder for the M1 scaffold.
export {};
