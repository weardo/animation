# Factory Decisions Log

Append-only. One entry per significant decision/learning: **Context → Decision → Rationale → (Supersedes?)**. Maintained via the `refine-standard` skill.

---

## 2026-06-22 — Render host: Remotion
**Context:** Need code-driven, deterministic, audio-capable, batch-able rendering with minimal plumbing; solo dev (license a non-issue).
**Decision:** Remotion is the host/orchestrator/encoder; Scene IR is its `inputProps` (no translation layer). Other renderers run *inside* it.
**Rationale:** Built-in deterministic render + audio + FFmpeg mux + batch; most LLM-authorable (React); deletes the need for a custom frame-capture loop.

## 2026-06-22 — Rigs: DragonBones with full mesh deformation
**Context:** Need identity-stable, code-generable skeletal rigs with organic deformation; no editor lock-in / per-seat cost.
**Decision:** Adopt DragonBones JSON format + `pixi-dragonbones` runtime in a Pixi canvas inside Remotion (committed render path), with **full mesh deformation (FFD)** in scope.
**Rationale:** Free/MIT, standard, IK + mesh deform built in; FFD covers bendy/blobby warping. Pixi-canvas chosen over pure-SVG specifically to get mesh deform. Supersedes the earlier "Spine-derived custom schema" idea.

## 2026-06-22 — Scene IR is a Lottie superset; Zod is the single source of truth
**Context:** Avoid inventing an animation IR.
**Decision:** Scene IR adopts Lottie's `{a,k}` property/keyframe model (round-trippable) and extends only where Lottie can't reach (rig refs, camera-parallax, generators, audio, morph/effects). Define all IR in **Zod** → TS types + runtime validation + JSON-Schema (for the future LLM front-end).
**Rationale:** Reuse a Linux-Foundation standard; one definition does three jobs.

## 2026-06-22 — Organic motion via a generator family
**Context:** Keyframes + morph can't express water, fire, neuron bead-strings, crowds.
**Decision:** A `generator` layer family — parametric components, pure functions of `(params+seed+frame)`, in an extensible registry. Water = one generator; neurons = another.
**Rationale:** Don't special-case effects; one mechanism, deterministic, future-proofs fire/smoke/clouds/crowds.

## 2026-06-22 — Library: content-addressed, versioned, lockfile-pinned; fractal reuse
**Context:** Reuse characters/clips/scenes across many videos as the library grows, without changing old videos.
**Decision:** `name@version` → content hash (`object-hash`), `library/index.json` catalog, `animation.lock` pinning. Reusable kinds at every granularity (parts → rigs → presets → clips → scene-templates → videos) via Remotion nested compositions.
**Rationale:** Package-manager patterns (not a custom registry); lockfile reconciles "growing library" with "deterministic renders."

## 2026-06-22 — Effects/post and audio are parallel, event-driven stacks
**Context:** Need After-Effects-style polish and sound design without losing sync.
**Decision:** Layer `effects[]` + composition `post[]` (reuse SVG/Pixi/Remotion FX, `@remotion/transitions`, `@remotion/motion-blur`); audio mixing + SFX-from-animation-events. Both driven by the same animation events. Motion blur is a StyleKit default.
**Rationale:** One event timeline drives picture and sound → coherence; everything reserved-in-IR-now, built M2/M3.

## 2026-06-22 — A video is a reproducible PROJECT bundle (standards-grounded, not invented)
**Context:** Video artifacts were scattered (source in examples/, scene.json ephemeral in .cache/, a single global animation.lock, out.mp4 at root) — no reproducible per-video unit. Checked for standards rather than inventing.
**Decision:** Adopt the convention shared by **OpenTimelineIO `.otiod`/`.otioz` bundles** and **dotLottie `.lottie`**: a root **manifest** + a **timeline/scene doc** + organized **media**, in a working DIRECTORY form (zip-able later). A project = `projects/<id>/{project.json, story.yaml, scene.json, project.lock, media/}`. CLI is project-centric: compile (`render <story> --project <id>`) vs reproduce (`render <id>` → renders the pinned scene.json, verified byte-identical). Per-project lock replaces the global one. `scene.json` promoted from throwaway cache → tracked, deterministic engine input.
**Rationale:** library : project :: npm package : app (third pillar after engine + library, ADR-001). The dir-bundle is OTIO-export-able and shareable as a zip (future `factory bundle`). Tracked: manifest/source/scene/lock (reproducibility); media/ gitignored (regenerable). Sources: OTIO `.otiod`/`.otioz`, dotLottie spec.

## 2026-06-22 — Software GL (swiftshader) balloons the disk; use 'angle' for procedural scenes
**Context:** Renders filled the root partition (~26GB) then crashed, then freed — for both the user and the agent. Root cause: `chromiumOptions.gl: 'swiftshader'` (software renderer) ballooned Chromium's Service-Worker **CacheStorage** to ~26GB during render, held open in the chrome temp profile (released on process death → the "fills then frees" symptom). Caught by monitoring `df` live during a render and killing before crash.
**Decision:** Use `gl: 'angle'` (hardware) for the render. Procedural characters are pure SVG/DOM, so determinism is DOM-based (not GL-based) — 'angle' is byte-identical across cold runs AND fast (~28s vs crash). Verified: two renders decoded-MD5 identical, disk flat at 31G. Software GL was only needed for WebGL/DragonBones FFD determinism — another reason to prefer the procedural provider. A future WebGL-determinism need must first solve the cache bloat (capped/disabled Chromium cache).
**Process lesson (→ verify-render):** when a render hangs/crashes, monitor `df -h /` live; "fills then frees on crash" = a process holding large unlinked temp open. Orphaned renders survive killing the npm wrapper — `pkill -9 -f compositor-linux`/`chrome-headless` too.

## 2026-06-22 — blip rebuilt procedurally; first real "procedural" provider (ADR-001)
**Context:** The code-generated DragonBones `blip` rendered broken — the FFD body mesh collapsed to a wedge and limbs misaligned. The *procedural art* (atlas parts) was actually clean; the breakage was 100% in the DragonBones skeleton/FFD assembly (hand-synthesizing a vendor mesh without the editor is brittle).
**Decision:** Implement the ADR-001 `procedural` rig kind: `RigDef.kind` enum (`dragonbones|procedural`), library `format` → provider in `toRigDef`, and Scene.tsx dispatches to `<ProceduralRig>` (pure-SVG primitive-shape character, idle/breathe/blink/wave) vs `<RigLayer>` (DragonBones). `blip` is now procedural (`proc://blip`); removed the broken DragonBones blip files + `gen-blip-rig.mjs` + FFD probes. Procedural rigs are deterministic-by-construction AND fast (no Pixi/WebGL/swangle). Visually verified clean; frame byte-identical across processes.
**Process lessons (→ verify-render skill):** (1) ALWAYS look at a frame — determinism ≠ correctness (I called the broken blip "verified" off determinism alone). (2) Remotion caches the webpack bundle — `rm -rf node_modules/.cache .remotion` if a code change doesn't show. (3) The pipeline Scene-IR cache key omits library state — `rm -rf .cache` after library/provider changes.

## 2026-06-22 — M2.1 Shading & Depth compositor implemented (foreground, not workflow)
**Context:** The M2 workflow stalled with slow render/verify loops (6–10 min idles); finished M2.1 in the foreground instead.
**Decision:** Shading lives in `src/render/shading.tsx` (`SceneLook`, `ContactShadow`, `objectFilter`, resolvers) + `Scene.tsx`. Scene `light` defaults on (StyleKit `DEFAULT_LIGHT`); per-layer `shading` defaults on (`DEFAULT_SHADING`), background-exempt. Components: directional light wash + vignette (screen-space), per-object contact shadow (camera-tracked), silhouette-following rim/AO/glow via CSS `drop-shadow` (works on SVG layers AND the Pixi canvas). Visually verified on `blip`; a shaded frame is byte-identical across two separate processes (determinism held — static styles + deterministic anchor).
**Deferred (honest):** true per-silhouette form-shade overlay (approximated by the scene wash + rim for now); gradient fills (typed, not yet rendered); far-layer atmospheric tint. **Process lesson:** a tight build→render→look loop is far faster in the foreground than in a render-heavy background workflow (logged under ADR/standard).

## 2026-06-22 — Shading & Depth is compositional, not post-processing
**Context:** Kurzgesagt depth comes from each object carrying supporting gradient shapes (contact shadow, form shade, rim, AO, glow) consistent with one scene light — not from per-object filters.
**Decision:** Add a Shading & Depth model (spec §11.1): a scene-level `light` as single source + a default-on per-layer `shading` that auto-generates supporting gradient shapes from silhouette + light + `z`; gradients are first-class animatable fills. Reuse SVG gradients/lighting filters + Pixi filters. M2 (look); fields reserved in Scene IR now.
**Rationale:** A single scene light makes the whole scene read as one coherent lit space; auto-derivation avoids hand-authoring shadow shapes (the manual labor we're eliminating). Quality floor → default-on. **Supersedes** the earlier assumption that StyleKit's drop-shadow/glow `effects` filters covered Kurzgesagt depth — they don't.

## 2026-06-22 — Object detail is a render-budget problem; split by behavior
**Context:** Kurzgesagt objects are hundreds of tiny shapes each. Treating all detail as live SVG DOM would crater laptop render times.
**Decision:** Add a `scatter` generator for procedural detail (seeded, poisson-disc) and a detail×performance strategy (spec §10.1): bake static high-count detail (cached/content-hashed), Pixi `ParticleContainer` for animated high-count, `<symbol>`/`<use>` for motifs, bake surface-bound detail into rig textures (deforms via FFD), and a per-scene shape budget with logging (no silent truncation). Added as risk #6.
**Rationale:** Detail density is a rendering-budget question, not an art one; splitting by behavior keeps it deterministic and laptop-viable. Authored multi-shape assets already work; the strategy is M2 + ongoing.

## 2026-06-22 — Deterministic WebGL render recipe (corrects the M1 spike)
**Context:** M1 built and self-reported "byte-identical determinism PASS." Independent cross-run verification (a fresh render in a new process, comparing the *decoded video stream*) found the opposite: ~all frames differed with hardware GL, then ~3/150 differed after switching GL — the in-session check only compared two back-to-back renders, which masked it.
**Decision:** The verified recipe for byte-identical Pixi/DragonBones output across cold runs is: (1) `chromiumOptions.gl: 'swangle'` (software SwiftShader — hardware `'angle'` is non-deterministic across runs), (2) `preserveDrawingBuffer: true` on Pixi init, (3) **synchronous** `continueRender` after `app.render()` (rAF gating fights Remotion's frame clock). Result: two cold renders byte-identical, 0/150 frames differ.
**Rationale:** GPU float variance + a paint-commit race were the two non-determinism sources. **Supersedes** the spike's "angle + rAF retires the determinism risk" claim.
**Process lesson (→ verify-render skill):** test determinism across SEPARATE process invocations and compare the DECODED VIDEO STREAM (`ffmpeg -map 0:v -f md5`), not the container (it carries a wall-clock `creation_time`). Updated the skill accordingly.

## 2026-06-22 — Visual-language systems: transitions/match-cuts, kinetic typography, color-script
**Context:** Kurzgesagt storytelling relies on seamless idea-to-idea transitions (match-cuts), bold animated text, and an emotional color arc across beats.
**Decision:** Add three first-class systems (spec §11.2–11.4): (1) transitions incl. `morph-match`, `match-cut` (shared element continuity via `match:{from,to}`), and `camera-continuous` — reuse `@remotion/transitions` + `flubber`; (2) a `text` layer (split from `asset`) with kinetic-reveal presets, auto-fit (`@remotion/layout-utils`), count-up, and later narration sync (Whisper + `@remotion/captions`); (3) a color-script where Story-IR beats carry `mood`/`palette`, Scene-IR palette tokens are the single source for all fills/gradients/`light.color`, and palettes interpolate across transitions in OKLab (`culori`/`d3-interpolate`).
**Rationale:** All three generalize existing mechanisms (transitions/morph, layers+stagger, palette tokens) rather than adding subsystems; reuse-first; deterministic. M2 (M3 for narration-synced text). Fields reserved in IR now.

## 2026-06-22 — M2.2 Multi-scene storytelling + transitions implemented
**Context:** The pipeline lowered a Story IR into ONE scene; storytelling needs a multi-BEAT story to become N SEQUENCED scenes on the global timeline, rendered as one continuous film with transitions between them (spec §11.2).
**Decision (how it composes):**
- **Beats → scenes (1:1).** `src/pipeline/lower.ts` lowers each Story-IR beat into one Scene-IR scene. Beat duration resolves from `duration` (`{seconds}`/`{frames}`/bare-seconds → frames via fps), falling back to `DEFAULT_BEAT_SECONDS` (4s). The single-beat M1 case with no duration/override keeps its 5s `M1_CONFIG` slice, so M1 byte-output is unchanged. A beat's `show[]` gates whether its bead-string generator layer is emitted, so the film reads as a story (intro shows just the character; the network appears when introduced).
- **Sequencing on the global timeline.** Cumulative `at`: `at[i] = at[i-1] + dur[i-1] − overlap[i]`, where `overlap[i]` is the frames scene[i]'s `transition_in` overlaps the previous tail (0 for `cut`/none). Each beat's `transition` lowers to the scene's `transition_in` (the first beat's is dropped — nothing precedes it).
- **Transition composition.** `src/render/Composition.tsx` renders the whole `scenes[]` as one continuous film via **`@remotion/transitions` `TransitionSeries`**: a `.Sequence` per scene (length = `duration_frames`) plus, before every scene after the first whose `transition_in` is non-`cut`, a `.Transition` of `transition_in.duration` overlap. `fade`/`wipe`/`slide` use their dedicated `@remotion/transitions` presets; `iris`/`mask`/`morph-match`/`match-cut`/`camera-continuous` fall back to `fade()` for now (proper SVG-mask / flubber / shared-element compositors are later-milestone work). Every transition is timed by `linearTiming({ easing: StyleKit "smooth" bezier })` so no transition is linear (spec §9). A `cut`/absent transition emits NO `.Transition` (segments butt = hard cut).
- **Total-duration computation.** `config.duration_frames = Σ scene.duration_frames − Σ transition overlaps` (computed in the lowering pass; `DEFAULT_TRANSITION_FRAMES = 15` when a non-`cut` transition omits `duration`). `Root.tsx`'s `calculateMetadata` independently recomputes the same sum and **throws** on mismatch (no silent truncation) — the lowering math and the rendered `TransitionSeries` length must agree.
**Determinism notes:** `TransitionSeries`/`linearTiming` are pure functions of Remotion's frame clock; the procedural provider is SVG/DOM, so the whole film stays deterministic on `gl:'angle'` (no software-GL disk bloat). Verified per the verify-render skill: rendered `projects/blip-story` (4 scenes — fade·fade·slide, 366f) in TWO separate processes and the **decoded video streams were byte-identical** (`ffmpeg -map 0:v -f md5`); frames extracted to /tmp and visually confirmed (fade crossfade at the b1→b2 boundary, slide seam at b3→b4). `projects/neuron-story` (3 scenes) also renders. typecheck clean.
**Files:** schema already reserved the fields (`src/ir/scene.ts` `TransitionSchema`/`transition_in`/multi-scene `scenes[]`; `src/ir/story.ts` `StoryTransitionSchema`/beat `transition`+`duration`); this milestone wired the lowering, compositor, and Root cross-check.

## 2026-06-22 — Build by milestone; M1 first
**Context:** Avoid building ahead of validated architecture.
**Decision:** M1 = minimal vertical slice proving every seam (script→mp4, DragonBones rig w/ mesh deform + bead-string generator + parallax camera, deterministic render, library+lockfile). M2/M3 staged in spec §15.
**Rationale:** If the M1 clip renders deterministically, the architecture is proven; the rest is feature work.

## 2026-06-22 — `scatter` generator: procedural density (§10.1)
**Context:** The Kurzgesagt look needs "hundreds of tiny shapes" density — starfields, dust, foliage, sparkle, crowds — as an ambient layer that composites over rigs/other generators. Keyframes and the bead-string can't express this; §10.1 called for a `scatter` generator + a shape budget.
**Decision:** Added `src/generators/scatter.tsx` as the second generator, following the bead-string contract exactly: a React `FC<GeneratorComponentProps>` that reads `useCurrentFrame()`/`useVideoConfig()` unconditionally then overrides with props, delegating to a **pure `renderScatter(props)`** (unit-testable, no hooks). Params are loose at the IR boundary and narrowed by `ScatterParamsSchema` (`.strip()`, every field defaulted) parsed at render time; colors resolve via `resolveFill` (palette token OR hex). Registered under key `'scatter'` in `registry.ts` (the only wiring step) and exported from `generators/index.ts`.
- **Placement:** `distribution: 'grid'` (default) is a near-square jittered even grid (cols/rows ≈ √(count·aspect), per-cell seeded jitter = poisson-disc-like even coverage without a mechanical lattice); `'random'` is seeded uniform. Region defaults to the full composition; any of `{x,y,w,h}` may be set to bound a sub-area.
- **Per-element variation:** each element draws size/color/rotation/animation-phase from its own `mulberry32(mixSeed(seed, i+1))` sub-stream; placement uses a separate `mixSeed(seed,'plac')` stream so the two never correlate.
- **Motifs:** `dot` (`<circle>`), `star` (a 4-point sparkle `<path>`, translate+rotate), `blob` (reuses `path.ts` `blobPath` for an organic closed path with its own seeded wobble).
- **Animation:** per-element `twinkle` (opacity), `pulse` (scale), `drift` (Lissajous wander), `none` — all driven purely by `frame + phase` at `anim_speed` Hz (deterministic, neighbours out of step). Optional `glow` (SVG gaussian-blur halo) and layer `opacity`.
- **Shape budget (§10.1):** `SCATTER_SHAPE_BUDGET = 2000` (in `types.ts`); `count` above it is **clamped and `console.warn`-ed with the exact drop count** — never silently truncated (matches the "no silent caps" rule). Verified: count=5000 → 2000 rendered + a warning.
**Determinism:** pure function of (params, seed, frame); no `Date.now`/`Math.random`. Rendered as an absolutely-positioned full-size `<svg>` so it composites cleanly. Verified per `verify-render` on `projects/scatter-demo` (3 scenes: star starfield · dot dust · blob foliage, 264f): two SEPARATE cold renders (caches cleared) gave byte-identical **decoded** video streams (`ffmpeg -map 0:v -f md5` = `012293749759f11ef66ac81c869c3359`); frames extracted to /tmp and visually confirmed all three motifs + animations composite over the bead-string + rig. Render uses `gl:'angle'` (SVG/DOM determinism; software GL stays banned — disk balloon).
**Rationale:** One more registered module, zero IR/pipeline changes — proves the §10 "adding a generator = adding one module" claim a second time and lands the §10.1 procedural-detail half of the object-detail strategy (baking + Pixi instancing remain).

## 2026-06-22 — First-class `shape` layer: @remotion/shapes primitives + flubber morph (ADR-003 #1)
**Context:** ADR-003 backlog #1 (Tier A, deterministic). The `shape` layer was a placeholder (`src/render/Scene.tsx` rendered an empty `AbsoluteFill`); `MorphChannelSchema`/`GradientFillSchema` existed but were unused. "Families are sockets; libraries are plugs" — so the shape socket adopts free libraries rather than hand-rolling vector geometry/morphing.
**Decision (libraries adopted):**
- **`@remotion/shapes`** — the PRIMITIVE catalog. A `shape: { kind, …params }` descriptor (extended `ShapePrimitiveSchema`, `kind` ∈ rect/circle/ellipse/triangle/star/polygon/pie/heart, params `.passthrough()`) selects the matching maker in `geometryForPrimitive`; per-kind defaults keep a minimally-authored layer renderable. The maker returns a pure path `d` + intrinsic box → SVG `viewBox`.
- **`flubber`** — PATH MORPH. The `morph` channel (`animated(string)` of SVG `d` strings) is evaluated in `evalMorph`: locate the active `[a,b]` keyframe segment for the frame, then `flubber.interpolate(a.d, b.d, {maxSegmentLength:10})` at the StyleKit-eased segment progress. flubber resamples DIFFERING point counts (star→heart works). `morph` takes precedence over a `shape` primitive when both are present. flubber ships UMD with no real ESM entry, so we import the namespace and probe `.interpolate ?? .default.interpolate` to work under both the Remotion webpack bundle and tsx/Node.
- **`@remotion/paths`** — `getBoundingBox` frames the morph path (its coords can start anywhere) into the SVG viewBox.
**Fill / stroke:** `fill` is a solid (animated) colour OR a linear/radial gradient. Gradients lower to an SVG `<defs>` `<linearGradient>`/`<radialGradient>` whose id is `shape-grad-${layer.id}` — DETERMINISTIC from the layer id, so two shapes never collide and markup is byte-identical every run. Linear angle (deg) maps to objectBoundingBox unit-vector endpoints. Palette tokens resolve through `resolveFill` (shared with the generator family); animated colours interpolate with Remotion `interpolateColors`. `stroke` is an optional colour+width. The `{a,k}` transform (position/scale/rotation/opacity) is evaluated with the same `eval.ts` StyleKit easing as every other layer.
**Wiring:** `src/render/Scene.tsx` dispatches `'shape'` → `<ShapeLayer>` inside the same `ParallaxWrapper` + §11.1 shading/effects wrappers as rig/generator, so shapes get depth + shading identically. `<ShapeLayer>` stays presentational (parallax/shading owned by `<Scene>`).
**Determinism:** pure SVG/DOM — no Pixi, no software GL; renders byte-identical on `gl:'angle'`. flubber is a deterministic geometric interpolator; gradient ids are content-derived. Verified per `verify-render`: typecheck clean; two SEPARATE cold render processes of `projects/shapes-demo` (2 scenes — a 5-primitive constellation with solid/linear/radial fills + strokes, then a star→heart morph with fill tracking gold→blue, 162f) gave byte-identical **decoded** video streams (`ffmpeg -map 0:v -f md5` = `3e83a6852fcb8d17dd8f6c14f7bfa5bd`); frames extracted to /tmp (primitives @30, mid-morph @92 = a genuine flubber in-between, heart @120) and visually confirmed.
**Files:** `src/ir/scene.ts` (already had `ShapeLayerSchema` with `shape`/`morph`/`fill`/`stroke`/`transform` reserved) · `src/render/ShapeLayer.tsx` (new renderer) · `src/render/Scene.tsx` (dispatch) · `examples/shapes-demo.yaml` + `projects/shapes-demo` (vertical slice) · deps `@remotion/shapes`/`@remotion/paths`/`flubber` in `package.json`.
**Rationale:** Adopting catalogs into the family (zero engine/IR change beyond the reserved descriptor) lands shapes + morph as a shippable, deterministic Tier-A increment, consistent with ADR-001/ADR-003.

## 2026-06-22 — Generators expansion: water, particles, fire, crowd (ADR-003 #4)
**Context:** ADR-003 backlog #4 (Tier A, deterministic). After bead-string (M1) and scatter (§10.1), the generator family needed the core ambient-motion catalog — water surfaces, particle fields, fire/smoke, and crowds — to cover spec §10's initial set. Each is a self-contained `generator` layer; "adding a generator = adding one module, no IR/pipeline change."
**Decision (how they were built):**
- **Four disjoint files, built in parallel.** Each generator is its own `src/generators/<name>.tsx` (`water`, `particles`, `fire`, `crowd`) with NO shared edits, so the four could be authored concurrently without merge contention. The ONLY shared touch — registering all four in `src/generators/registry.ts` — was done as a single **sequential integrate** step after the parallel build, keeping the registry's import list the one serialization point.
- **Contract match (each follows scatter/bead-string exactly):** a React `FC<GeneratorComponentProps>` that calls `useCurrentFrame()`/`useVideoConfig()` unconditionally then overrides with explicit props, delegating to a **pure `renderX(props)`** (no hooks → unit-testable). Params are narrowed by the generator's OWN in-file Zod schema (`.strip()`, every field defaulted — NOT added to `types.ts`); colors resolve via `resolveFill` (palette token OR hex). Seeded RNG only (`mulberry32`/`mixSeed` + `simplex-noise`); rendered as one absolutely-positioned full-size `<svg>`.
- **Closed-form looping lifecycle for determinism (the load-bearing rule).** Every animated value is a closed-form function of (frame, index, seed) with a looping lifecycle — NO accumulation/integration across frames. Remotion renders frames out-of-order across workers, so any per-frame state carry would diverge. Concretely: water crest height = `sin(x·k − frame·ω) + chop·simplex(x, band, t)`; particle progress = `((frame·speed + index·offset) mod lifespan)/lifespan` with a raised-cosine fade window seamless at the loop seam; fire smoke puffs wrap a modulo phase → rise/grow/drift/fade; crowd bob = `cos(t + phase)` and a narrow `((1+cos)/2)^k` blink spike.
- **Per-generator character:** `water` = stacked translucent scrolling wave bands (back bands slower = soft parallax) + crest-riding foam; `particles` = drift/up/down field with seeded lateral sway; `fire` = layered blobby teardrop flames in a warm radial gradient (deterministic ids from seed) + rising smoke; `crowd` = jittered-grid (or random) field of head-circle + body-blob creatures with idle bob + blink-squash.
- **Shape budget (§10.1)** in every one: `count`/foam/total clamped to a per-generator budget with a `console.warn` of the exact drop — never silently truncated.
**Determinism:** pure SVG/DOM (no Pixi, no software GL) → renders on `gl:'angle'`. Verified: `tsc --noEmit` clean; a Node smoke render of all four (`renderToStaticMarkup`) confirmed each produces markup, is byte-identical at a fixed frame across repeated calls, and differs across frames (animates) — water 63 / particles 120 / fire 10 / crowd 240 shape nodes at frame 12.
**Files:** new `src/generators/{water,particles,fire,crowd}.tsx`; `src/generators/registry.ts` (registered all four). Spec §0 generators line + §10 initial set and ADR-003 #4 marked ✅.
**Rationale:** Parallel disjoint files + a single sequential integrate is the repeatable pattern for batch-adding generators (no shared-file contention; one wiring point). The closed-form looping lifecycle is the determinism contract for any "flowing" generator and is now stated as such in the spec/ADR.

## 2026-06-22 — Determinism recipe corrected: CPU raster (no gl); + fast frame verification
**Context:** Procedural scenes used `gl:'angle'` (Intel Iris Xe iGPU). The generator batch exposed GPU raster as NON-deterministic for blur/alpha-heavy SVG (particles/fire/crowd differed cross-process ~47dB); crisp content (shapes/scatter/blip) was fine. Software `swiftshader` is deterministic but balloons the disk.
**Decision:** Render procedural scenes with **NO gl backend** (`chromiumOptions: {}`) → **CPU rasterization** — byte-deterministic (generators-demo identical across cold processes) AND disk-safe. **Supersedes "use gl:'angle'".** The iGPU is intentionally unused for determinism; DragonBones/WebGL would opt back into gl. concurrency=cpu-2; x264 'faster'; encode single-pass for byte-identical mp4.
**Lesson:** determinism is a property of CONTENT + BACKEND, not just code — the generators' JS was deterministic in isolation; the GPU raster backend was the culprit. The workflow repair loop couldn't fix it (no code bug to fix).
**Fast verification:** full complex videos are slow (~180s/12s-video). Dev/verify loop uses `render <proj> --frames auto` → ~5 PNG stills (~14s, no encode), byte-identical to the video frames. Verify on FRAMES; full video only for final.

## 2026-06-22 — Render tiers: CPU (byte-exact) default + GPU (perceptual) opt-in; perceptual-diff
**Context:** GPU (iGPU 'angle') is ~3.5x faster (52s vs 180s) but only *perceptually* identical run-to-run, not byte-identical — quantified at SSIM 0.99879 / PSNR 56dB (well above the 40dB "visually lossless" bar) = imperceptible. Determinism-as-byte-identical was a stricter proxy than needed for most uses (caching keys on the IR hash; regression can be perceptual; only cross-machine repro needs byte-exact).
**Decision:** Two render tiers. **CPU (default, `chromiumOptions:{}`)** = byte-exact, the canonical/portable record + golden tests. **GPU (`--gpu`, gl:'angle')** = fast, verified PERCEPTUALLY via `tools/perceptual-diff.mjs` (SSIM≥0.99, PSNR≥40). Use GPU for speed/previews/local; CPU for the shareable record + cross-machine reproducibility. This partially un-gates ADR-003 Tier-B: GPU is usable NOW with perceptual verification, not blocked. (Cross-MACHINE GPU determinism still isn't guaranteed → CPU remains the portable bar.)

## 2026-06-22 — VMAF is the primary perceptual metric (GPU-tier quality gate)
**Context:** PSNR is pure pixel math and can be fooled (a uniform blur scores low yet looks fine). VMAF (Netflix; learned perceptual model) is the modern standard; ≥90 = "excellent". ffmpeg n8.1.2 here ships --enable-libvmaf + bundled models (vmaf_v0.6.1 default).
**Decision:** `tools/perceptual-diff.mjs` (npm run compare) uses VMAF as the PRIMARY gate (≥90), SSIM/PSNR as secondary signals. The GPU render tier is verified perceptually with VMAF; CPU tier stays byte-exact. Measured GPU run-to-run = VMAF 97.5 / SSIM 0.99879 / PSNR 56dB → EXCELLENT (variation perceptually negligible).

## 2026-06-22 — Plugin foundation: minimal core + capability via plugins (ADR-005, DONE)
**Context:** ADR-005 — generalize ADR-001 (providers) and ADR-004 (generators) into ONE mechanism: a small generic engine core that owns extension-point registries, with capability (code) contributed by plugins and content (data) in the library. The seeds were the static `src/generators/registry.ts` name→component map and the `rigDef.kind === 'procedural' ? … : …` branch in `src/render/Scene.tsx`. Goal: build the foundation incrementally and dogfood it by migrating the built-ins, keeping every committed demo byte-identical.
**Decision (what was built):**
- **Engine core** `src/engine/`: a generic `Registry<T>` (register last-wins / has / names / `get` THROWS loudly on unknown — no silent fallback, matching the old generator-registry contract) · SEVEN named extension points — three LIVE (`generators`, `rigProviders`, `characterStyles`) + four STUBS (`effects`, `transitions`, `layerTypes`, `passes`, defined with the right shape, no contributors/consumers) · the `EngineAPI` (the single object handed to `register(api)`; one `register*` method per point — a plugin never imports a registry directly) · the `Plugin`/`plugin.json` contract validated by a Zod `PluginManifestSchema` (id, version, `contributes[]`, `deps?`, license, provenance — Zod is the single source per CLAUDE.md r.3) · `loadPlugins()` (re-validate each manifest → topological `deps` order, cycle/missing-dep throws → `register(api)`; returns applied order + a per-registry contribution snapshot) · `ENABLED_PLUGINS` config.
- **Built-ins migrated to CORE PLUGINS** `plugins/`: **core-generators** (bead-string/scatter/water/particles/fire/crowd → `generators`) · **core-rigs** (thin adapters: `procedural`→`<ProceduralRig>`, `dragonbones`→`<RigLayer>` → `rigProviders`) · **blob-creature** (today's `characterMarkup`, signature already = `CharacterStyleBuilder`, → `characterStyles` under id `blob-creature`).
- **Consumers rewired to delegate** (no parallel systems): `src/generators/registry.ts` keeps its helper API but delegates to the engine `generators` registry · `src/render/Scene.tsx` resolves the rig via `rigProviders.get(rigDef.kind)` (hardcoded branch deleted) · `src/render/ProceduralRig.tsx` + `src/cli/factory-gen.ts` resolve the style via `characterStyles.get(spec.style)` · CharacterSpec gained an optional `style` field defaulting to `blob-creature` (existing specs unchanged) · `src/render/index.ts` (Remotion bundle entry) + `factory-gen` call `loadPlugins()` before render so the resolved capability set is identical runtime vs offline and across cold processes.
**Determinism (the load-bearing property):** every `register` is pure data wiring (no clock, no per-frame state); the resolved capability set is a pure function of `ENABLED_PLUGINS`, loaded once before render → identical across cold processes. Verified per `verify-render`: `tsc --noEmit` clean; the five committed projects (blip-intro, blip-story, generators-demo, scatter-demo, shapes-demo) + neuron-demo (exercises the dragonbones provider) rendered via `--frames auto` in TWO SEPARATE cold processes (CPU raster, `.cache`/`.remotion`/`node_modules/.cache` cleared between) gave byte-identical frame md5s for all six; the pip factory preview PNG is byte-identical (only the spec's new `style` field changes its content hash — legitimate new data, not a render change).
**Lesson / pattern:** the dogfood test — migrate the existing built-ins THROUGH the new seam and require byte-identical demos — is what proves a plugin foundation is a pure refactor, not a behaviour change. Future capability (effects/transitions/text/color/styles/passes; ADR-003 Tier-A backlog) is now authored AS A PLUGIN contributing into the matching (already-defined) extension point — never a core edit. ADR-004's generator-PRESET layer stays a separate backlog item (presets are library data; the core-generators implementations are their code peer).
**Files:** new `src/engine/{registry,api,plugin,loader,enabled,index}.ts` · new `plugins/{core-generators,core-rigs,blob-creature}/{plugin.json,index.ts(x)}` · modified `src/generators/registry.ts`, `src/render/Scene.tsx`, `src/render/ProceduralRig.tsx`, `src/render/index.ts`, `src/factory/spec.ts` (style field), `src/cli/factory-gen.ts`. Docs: ADR-005 "Foundation status"/Backlog marked DONE; spec §0 (plugin-foundation subsection + Providers + Build status); CLAUDE.md (repo layout + plugin-model paragraph).

## 2026-06-22 — Pure engine: the core specializes in NOTHING — purge the 'character' domain entity (ADR-006, DONE)
**Context:** ADR-005 made *capability* pluggable but the core still hardcoded a **domain entity**: a `characterStyles` extension point, `CharacterSpec`/`characterMarkup` in `src/factory/` (core-adjacent), and a character-specific `factory-gen` CLI. A generated video is not necessarily a *character* video (it may be an infographic / chart / still / abstract motion); the engine must composite GENERIC layers only. "Character" had to become ONE provider plugin among many.
**Decision (what was built):**
- **Engine:** removed the `characterStyles` extension point; renamed `rigProviders` → one generic **`providers`** registry. `EngineAPI` drops `registerCharacterStyle`; exposes `registerProvider(id, component)` (a provider id — `blob-creature`/`dragonbones`/future `chart`/`widget` — NOT a "style"). `ProviderProps = { layer, rigDef, easings }`. The 4 stub registries are unchanged.
- **Out of core → `plugins/blob-creature/`:** `src/factory/` is **DELETED**. `spec.ts` (CharacterSpec + `parseSpec` + `BLIP_SPEC`) and `character.ts` (`characterMarkup`) moved into the plugin. The provider renderer (`plugins/blob-creature/renderer.tsx`, formerly core `src/render/ProceduralRig.tsx`) receives the rig layer's OPAQUE spec, validates it with the plugin's OWN `CharacterSpec` (Zod), and renders via `characterMarkup`. Core `src/ir` has ZERO `CharacterSpec` import — `RigDef.spec` is `z.record(unknown)` (provider validates).
- **IR + consumers:** `RigDef` = `{ uri, provider, spec? }` (was `{ uri, kind, spec }` + a separate `spec.style`). `src/library/loader.ts#toRigDef` sets `provider` from the catalog entry's `provider` field (else derives: `format:'procedural'` → `blob-creature`, else `dragonbones`) and embeds the opaque spec for the blob-creature path. `src/render/Scene.tsx` rig dispatch → `providers.get(rigDef.provider)` (the old `kind` branch is gone). `core-rigs` registers the vendor renderer under `dragonbones`. `src/pipeline/lower.ts` fallback rig emits `provider:'dragonbones'`. `src/library/catalog.ts` `CatalogEntry` gained an optional `provider` field. `src/cli/render.ts` vendoring keys off `def.provider`.
- **Factory:** the character-specific generator moved to `plugins/blob-creature/generator.ts` (CharacterSpec → library entry + `rsvg` preview + content-addressed catalog entry `kind:rig, provider:blob-creature`). `src/cli/factory-gen.ts` is now a thin CLI that dispatches to the active provider plugin's `generate(...)` (Node-only; deliberately NOT re-exported from the browser-bundled plugin `index.tsx`).
- **Wire-compat:** the `rig` layer type, `defs.rigs`, and `library/characters/` namespace stay (data org). All 6 demo projects were RE-LOWERED so their `scene.json` carries the new `{provider, spec}` rig def shape.
**Determinism (the load-bearing property):** a pure relocation, not a behaviour change. Verified per `verify-render`: `tsc --noEmit` clean; `render blip-intro --frames auto` (CPU raster) in TWO SEPARATE cold processes (`.cache`/`.remotion`/`node_modules/.cache` cleared between — the stale-pipeline-cache gotcha matters here since the provider field changed) gave byte-identical frame md5s across all 5 stills (blob-creature provider path); `neuron-demo` renders correctly (dragonbones provider path); blip + neuron frames eyeballed for correctness. Core grep for `CharacterSpec`/`characterStyle`/`rigProviders`/`ProceduralRig` returns only migration-explaining comments — no live code refs.
**Lesson / pattern:** the same dogfood test as ADR-005 (push the built-in THROUGH the new seam, demand byte-identical demos) proves purification is a refactor. The endpoint of ADR-001/004/005: **core (generic) · plugins (capability/code) · library (content/data)** with NO domain leakage into core. A chart/infographic pipeline now drops in as a provider plugin with a `ChartSpec` + its own `generate(...)` — zero core change, exactly like blob-creature.
**Files:** new `plugins/blob-creature/{spec.ts,character.ts,renderer.tsx,generator.ts,index.tsx}` · deleted `src/factory/{spec.ts,character.ts}` + `src/render/ProceduralRig.tsx` + old `plugins/blob-creature/index.ts` · modified `src/engine/{api.ts,registry.ts,index.ts,enabled.ts}`, `src/ir/scene.ts`, `src/library/{loader.ts,catalog.ts}`, `src/pipeline/lower.ts`, `src/render/{Scene.tsx,index.ts}`, `src/cli/{factory-gen.ts,render.ts}`, `plugins/core-rigs/{index.tsx,plugin.json}`, `plugins/blob-creature/plugin.json`, `library/index.json`, all 6 `projects/*/scene.json`+`project.json`+`project.lock`. Docs: ADR-006 status → DONE + Implementation section; spec §0 (plugin-foundation + Providers + Asset-factory + Build status); CLAUDE.md (repo layout + plugin-model + layer taxonomy).

## 2026-06-22 — Effects channel: the per-layer `effects[]` stack as the `core-effects` plugin (ADR-003 #2, DONE)
**Context:** ADR-003 #2 — give layers a composable, animatable effect stack WITHOUT inventing an effects engine ("families/registries are sockets; free libraries are plugs"). The Scene IR already reserved `effects?: EffectSchema[]` (loosely `{kind, ...params}`) on every layer; the engine had a STUB `effects` registry (ADR-005) with no contributors/consumers. The build had to be a PLUGIN populating that extension point — never a core edit (ADR-005/006: capability is a plug, core is generic). Determinism is the gate: SVG/CSS filters + `@remotion/motion-blur` are Tier-A (frame-deterministic on the CPU raster default); WebGL effects would be Tier-B (non-deterministic + disk-balloon) and are explicitly out of scope here.
**Decision (what was built):**
- **Engine contract (generic, in core):** `EngineAPI.registerEffect(kind, impl)` writes into the `effects` `Registry<EffectImpl>` (following the existing `register*` pattern). The core owns ONLY the generic contract — `EffectImpl = { parse(params)→typed, build(params, frame)→EffectContribution }` and `EffectContribution = { filterPrimitives?, cssFilter?, wrap? }` — and knows NOTHING about any specific effect (no domain leakage; every effect lives in the plugin). `build` is a PURE function of (validated params, frame): the frame is passed IN so animatable effect params resolve deterministically; the effect never reads a clock/RNG.
- **`core-effects` plugin** (`plugins/core-effects/{plugin.json,index.ts}`, added to `src/engine/enabled.ts`): registers 9 effect kinds, each narrowed by its OWN in-plugin Zod (`.strip()`, all fields defaulted) — **blur** (feGaussianBlur), **glow** (feGaussianBlur→feColorMatrix flat-color flood→feMerge over source = emissive bloom), **drop_shadow** (CSS `drop-shadow`, silhouette-following like §11.1's objectFilter), **color_grade** (CSS brightness/contrast/saturate/hue-rotate, only emitting non-identity terms), **turbulence** + **displace** (feTurbulence+feDisplacementMap; `animate` nudges base frequency by a frame-driven delta = flowing ripple, still a pure fn of frame; fixed `seed`), **vignette** (radial-gradient overlay via `wrap`), **grain** (feTurbulence→feColorMatrix desaturate→feComposite-in→feBlend overlay; fixed `seed`), **motion_blur** (`@remotion/motion-blur` `<Trail>` via `wrap`).
- **Compositor** (`src/render/effects.tsx`, wired into `src/render/Scene.tsx#LayerView`): `resolveEffects(layerId, effects, frame)` walks `effects[]` IN ORDER, looks each up via the engine `effects` registry (`get` throws loudly on an unknown kind), validates+builds it, and COMPOSES the three channels — SVG `filterPrimitives` CHAINED inside ONE `<filter id="fx-<layer.id>">` (deterministic id from layer id, mirroring the ShapeLayer gradient-id convention; each stage's `result` feeds the next, `SourceGraphic`→e0→e1→…), CSS `cssFilter` fragments concatenated (the `url(#id)` ref prepended), `wrap`s collected. `applyEffects` emits the `<filter>` def, wraps the node in a div carrying the combined `filter:` style, then applies each `wrap` outermost (last effect = outermost). Layers without `effects[]` are returned UNCHANGED (backward-compatible — all prior demos untouched). The authored stack is a SEPARATE wrapper around the sub-renderer, composing cleanly ON TOP of the default-on §11.1 shading drop-shadow + the parallax wrappers — the two never fight for the single CSS `filter` slot (shading stays on its own `AbsoluteFill`).
**Determinism (the load-bearing property):** registration is pure data wiring; every `EffectImpl.build` is pure over (params, frame); the filter id is content-derived; turbulence/grain seed feTurbulence with a fixed param. All Tier-A (no WebGL). Verified per `verify-render`: `tsc --noEmit` clean; `render effects-demo --frames auto` (CPU raster) in TWO SEPARATE cold processes (`.cache`/`.remotion`/`node_modules/.cache` cleared between) gave byte-identical md5s for all 5 stills; frames eyeballed — the glow bloom (blob head) and the motion_blur `<Trail>` smear (moving triangle) render correctly.
**Lesson / pattern:** the FIRST consumer of a STUB extension point (effects) — built as a plugin, verified byte-identical — proves the ADR-005 socket/plug design holds beyond the dogfooded built-ins: a brand-new capability plugged into a reserved-but-empty registry with ZERO core edit except adding the generic `registerEffect` seam + wiring the compositor consumer. The three-channel `EffectContribution` (SVG primitive / CSS fragment / subtree wrap) is the reusable shape for any look-op that must compose in stack order; future Tier-B GPU effects (bloom/glitch/CRT) plug in as another effects plugin behind the GPU-determinism gate without touching this contract.
**Files:** new `plugins/core-effects/{plugin.json,index.ts}` · new `src/render/effects.tsx` · new `examples/effects-demo.yaml` + `projects/effects-demo/` (demo bundle) · modified `src/engine/{api.ts,registry.ts,index.ts,enabled.ts}` (the `registerEffect` seam + `effects` registry now LIVE, no longer a stub), `src/render/Scene.tsx` (effects resolved+applied in `LayerView`), `src/pipeline/lower.ts`, `package.json`/`package-lock.json` (`@remotion/motion-blur`). Docs: ADR-003 #2 → DONE; spec §0 (Build status + NEXT-M2 line); CLAUDE.md (golden-rule reuse list already names `@remotion/motion-blur`).

## 2026-06-22 — Domain-agnostic compiler: the M1 demo was BAKED INTO the front-end; the delete-the-plugin test is the decoupling proof (ADR-007, DONE)
**Context:** ADR-006 genericized the BACK-END (compositor + provider dispatch + moved `CharacterSpec` out) and we declared a "pure engine." That was only half true — the project was built M1-first as a hardcoded neuron/character/dragon vertical slice, and two domain infections survived in the FRONT-END and in code LOCATION: (1) **the lowering pass WAS the demo** — `src/pipeline/lower.ts` hardcoded `M1_REFS = {background, beadStringPath, rig}`, a `buildBeadStringLayer()`, a forced `background+bead+rig` on every scene, `narrator`/`L_neuron`/`M1_RIG_ANIM='throw'`/`RIG_CLIP_PLANS[blip]`, and `bead-string` special-casing — the system could RENDER anything but only COMPILE the neuron demo; (2) **generator + rig CODE physically lived in core** (`src/generators/*`, `src/rig/*`) and the plugins were hollow re-import shells, so the dependency arrow still pointed plugin→core in name only; (3) the **Story IR hardcoded `characters`** as a first-class entity (the front-end analog of the ADR-006 back-end bug). The ADR-006 verify passed because its grep looked for the PREVIOUS disease (`CharacterSpec`/`characterStyles`), never for `bead-string`/`narrator`/`M1_REFS`/`src/generators`.
**Decision (what was built):**
- **Lowering is now a generic `StoryIR→SceneIR` transform:** layers are built ENTIRELY from each beat's `show[]` (asset/generator/shape/rig, dispatched by which field is set); nothing is force-injected — a background/generator/rig appears iff the story declares it. All M1 constants/builders deleted; per-scene defaults (`idle` clip, seconds×fps duration) are GENERIC. `show[].args` flow straight through, validated by each family's own Zod at render time (sockets/plugs).
- **Story IR demoted `characters` → generic `cast`:** a story names refs (`cast: {name: {ref, provider?, palette?}}`); a `show[].actor` binds a layer to a cast key. "Character" is just a cast entry whose ref resolves to a rig — NOT a schema entity. No `neuron`/`bead`/`character:` vocabulary.
- **Plugin code physically relocated** to `plugins/core-generators/` + `plugins/core-rigs/`; core keeps only the generic sockets (`GeneratorLayer` + `GeneratorComponentProps` + provider-dispatch). The plugin manifest (`plugins/enabled.ts`) + composition root (`render-entry.tsx`) live OUTSIDE `src/`.
- **Generic CLI lock:** `lockRefsForScene` walks the compiled scene's `defs` (not a hardcoded triple).
- **Last provider-name leaks purged from core** (the ones ADR-006 left in loader/render): `src/library/loader.ts` REQUIRES the catalog entry's `provider` (no `format→provider` guess naming `blob-creature`/`dragonbones`) and embeds an inlined spec keyed on the `proc://` URI SCHEME; `src/cli/render.ts#vendorAssets` keys source vendoring on URI scheme, not provider id. Provider knowledge is now DATA (catalog `provider:` field) + plugin code, never core logic.
**Determinism (the load-bearing property):** lowering stays a pure `(story, opts)→SceneIR` (seeds derive from the story hash; StyleKit easing refs; no clock/RNG). Verified per `verify-render`: `tsc --noEmit` clean; `render neuron-demo/generators-demo --frames auto` (CPU raster) across TWO cold processes → byte-identical stills; blip + neuron frames eyeballed correct. NOTE: output legitimately DIFFERS from pre-ADR-007 bytes — scenes are now author-declared, so we verify REPRODUCIBILITY + correct visuals, not equality with the old output (an explicit ADR-007 carve-out).
**Lesson / pattern (TWO durable proofs):**
1. **The "M1-demo-in-the-compiler" leak:** building a pipeline "M1-first as a vertical slice" silently bakes the demo's SUBJECT into the compiler's transform passes, not just its renderers. A back-end-only purity grep (ADR-006) misses it. The front-end (parse→lower) and code LOCATION must be audited for domain words too — a compiler that can render anything but only compile one story is NOT generic.
2. **The DELETE-THE-PLUGIN test is the decoupling proof** — stronger than any grep. Moving a built-in plugin dir aside and re-typechecking `src/` (must stay EXIT 0) mechanically proves the dependency arrow points plugin→core, never core→plugin; a hollow re-import shell (plugin that just `import { X } from '../../src/...'`) FAILS this test. Pair it with `grep "from .*plugins/" src/` (must be empty). Both are now permanent `verify-render` gates.
**Files:** modified `src/pipeline/lower.ts` (generic), `src/ir/story.ts` (`cast`), `src/cli/render.ts` (`lockRefsForScene` + scheme-keyed vendoring), `src/library/loader.ts` (require `provider`, scheme-keyed spec embed), `src/library/catalog.ts`, `src/render/Scene.tsx`/`ShapeLayer.tsx`/`index.ts`, `src/engine/{api,registry,index,generator,loader}.ts` · new (relocated) `plugins/core-generators/*`, `plugins/core-rigs/*`, `plugins/enabled.ts`, `render-entry.tsx`, `src/render/GeneratorLayer.tsx`, `src/engine/generator.ts` · `library/index.json` (dragon `provider: dragonbones`) · re-authored `examples/*.yaml` + re-compiled all `projects/*/`. Docs: ADR-007 → DONE; spec §0; CLAUDE.md (repo layout — `src/generators`/`src/rig` removed from core); `verify-render` (+2 gates).

## 2026-06-22 — StyleKit + camera are selectable DATA: the core is neutral on STYLE (ADR-008 I2–I4, DONE)
**Context:** ADR-008 I1 (output format) was already done; the last "agnostic" blockers were that the Kurzgesagt LOOK was hardcoded in core (`src/render/stylekit.ts` held the palette/easings/spring/idle/breathing/blink/light/shading VALUES + the quality-floor as an always-on mandate) and the camera RECIPE table (slow_push_in/pan_*/hold magic numbers) lived in `src/pipeline/camera.ts`. A general factory must do flat/technical/corporate looks + arbitrary camera, not just Kurzgesagt — so the VALUES had to become selectable DATA while core keeps only the generic SCHEMA + mechanism.
**Decision (what was built):**
- **I2 — StyleKit as data.** The PURE `StyleKitSchema` lives in `src/ir/stylekit.ts` (no remotion dep, so the Scene IR can carry it): palette tokens · `easings`/`defaultEasings` · `motion`{spring/springHeavy/springBouncy/idle/breathing/blink/stagger/parallax/motionBlurShutter} · `light` · `shading` · a `floor`{liveness,parallax,shading,nonLinearMotion} bool object. `src/render/stylekit.ts` keeps ONLY: a re-export of that schema/types, the easing HELPER fns (`easingFn`/`bezierFn` → Remotion `Easing.bezier`, NEVER reimplemented), and `NEUTRAL_STYLEKIT` (generic greys + standard CSS easings + floor OFF; NO Kurzgesagt hex / liveness magic numbers). The Kurzgesagt VALUES moved verbatim to `library/stylekits/kurzgesagt.json` (catalog `kind:stylekit`, `uri:stylekit://kurzgesagt`); the loader gained `toStyleKit(ref)` (parses `library/stylekits/<name>.json` via the schema, like `proc://` for rigs). Story IR gained optional top-level `style` (default `DEFAULT_STYLEKIT_REF = "kurzgesagt"`). Lowering's `resolveStyleKit` resolves it through the Library, (a) seeds `defs.palette`/`defs.easings` from it AND (b) carries the WHOLE resolved kit in the Scene IR as `defs.stylekit` (the Scene-IR schema was extended). Render-time (`Scene.tsx`/`shading.tsx`/both providers via `ProviderProps.stylekit`) reads motion/liveness/shading/floor from `defs.stylekit` — NO core look constant survives at render. Standalone (no Library) → `NEUTRAL_STYLEKIT`.
- **I3 — floor opt-out.** `library/stylekits/plain.json` (floor flags all OFF, neutral light-grey palette). The render HONORS each toggle as a generic mechanism switch: `shading=false`→`Scene.tsx` skips ALL §11.1 shading (object filter + contact shadow + scene light wash); `parallax=false`→every layer rides the camera flat (no `cameraPosition*(1-parallax)` counter-shift); `liveness=false`→both providers skip idle/breathe/blink (`BlobCreatureProvider` static neutral pose; `RigLayer` no overlays); `nonLinearMotion=false`→linear easing allowed.
- **I4 — camera as data + arbitrary keyframes.** (a) The Story-IR `CameraIntent` accepts an object with explicit `position`/`zoom` `{a,k}` channels; `cameraFromIntent` passes them straight through verbatim (strict Scene-IR boundary validates them) — any move, not just a preset. (b) The recipe magic numbers (hold/establishing/slow_push_in/slow_pull_out/pan_left/pan_right) moved out of `src/pipeline/camera.ts` into `library/camera/presets.json` (each a from→to position+zoom ramp + a `default` + an `easing` token); `camera.ts` reads the table ONCE at module load and keeps only the generic from→to expansion — zero recipe magic numbers as the SOURCE of truth.
**Determinism + value-preservation (the load-bearing properties):** verified per `verify-render` on CPU raster across TWO cold processes — `character` (default kurzgesagt), `plain-demo`, `custom-camera-demo` all byte-identical cross-process. STRONG value-preservation check: the default `character` demo renders **byte-identical to pre-refactor HEAD f7dfefd** (rendered from a `git worktree` of HEAD) on all 5 stills — the Kurzgesagt values were RELOCATED, not changed. `plain-demo` DIFFERS from the kurzgesagt render on every frame (floor toggles have visible effect); `custom-camera-demo` shows the authored diagonal pan + punch-in zoom (creature off-frame at t=0 → centered at t=119). typecheck clean; domain-clean + delete-the-plugin + no-plugin-import gates still green.
**Lesson / pattern:** the "look" is the THIRD axis of agnosticism (after FORMAT and DOMAIN) — and the same delete-the-plugin spirit applies: a house style is CONTENT (library data carried in the IR), never a core edit. The reusable shape is "schema in core (`src/ir`), values in library DATA, resolved by a pass, carried in the IR, read at render via props" — identical to how `proc://` rigs and the catalog `provider:` field work. The quality FLOOR became a per-kit OPT-OUT bool object rather than an always-on mandate, so flat/technical/diagram looks are a DATA selection (`style: plain`), not a code fork. **Value-preservation via a `git worktree` render of the pre-refactor HEAD is the strongest proof a "relocate values to data" refactor changed nothing** — byte-identical stills, not just "looks the same." A near-black shadow/vignette tint that is SCALED by the kit's light (not a palette token) is legitimately mechanism, not style, so it may stay in `src/` — the style-clean gate targets the SIGNATURE palette hexes + the liveness magic numbers, which now live only in the JSON.
**Files:** new `src/ir/stylekit.ts` (the PURE schema) · rewrote `src/render/stylekit.ts` (re-export + helpers + `NEUTRAL_STYLEKIT`) · new `library/stylekits/{kurzgesagt,plain}.json` + `library/camera/presets.json` · rewrote `src/pipeline/camera.ts` (reads the DATA table, no magic numbers) · modified `src/ir/{story.ts (optional `style`),scene.ts (`defs.stylekit`),index.ts}`, `src/pipeline/lower.ts` (`resolveStyleKit`/`DEFAULT_STYLEKIT_REF`/seed+carry the kit), `src/library/loader.ts` (`toStyleKit`), `src/render/{Scene.tsx,shading.tsx,Composition.tsx,Root.tsx,ShapeLayer.tsx,eval.ts}` (read `defs.stylekit`, honor `floor`), `src/engine/api.ts` (`ProviderProps.stylekit`), `plugins/core-rigs/{index.tsx,RigLayer.tsx,liveness.ts,animated-eval.ts}` + `plugins/blob-creature/{renderer.tsx,character.ts}` (liveness magnitudes from the kit; honor `floor.liveness`), `library/index.json` (stylekit entries). New `examples/{plain-demo,custom-camera-demo}.yaml` + `projects/{plain-demo,custom-camera-demo}/`. Docs: ADR-008 → I1–I4 DONE; spec §0 (new "Style is selectable library DATA" subsection + Build status line); CLAUDE.md (golden rule 7 reworded — quality floor is the DEFAULT stylekit, not a mandate); `verify-render` (+ style-clean gate).

## 2026-06-22 — Text/typography = a core generic layer; fonts are vendored DATA (never CDN)
**Context:** Text is the first capability after the agnostic cleanup. It is a UNIVERSAL primitive (like shape), not a domain/subject — so it is a CORE generic layer kind (asset/shape/text/generator/rig/clip), built as thin adapters over Remotion (@remotion/layout-utils fitText; kinetic presets on interpolate/spring) — never reimplementing a primitive (ADR-003).
**Decision:** `text` layer (IR + lowering buildTextLayer + src/render/TextLayer.tsx, mirroring shape) with presets none/fade/rise/stagger/typewriter/count_up. Color from the palette/stylekit; honors defs.stylekit floor toggles. FONT DETERMINISM: vendor a LOCAL OFL font (DejaVuSans, in public/fonts + library/fonts) loaded via staticFile + font-readiness before paint — NEVER @remotion/google-fonts (network + non-deterministic + violates local-first). Verified: text-demo byte-identical cross-process (font ready before paint), crisp + laid out, count-up animates.
**New determinism surface:** font glyphs must be present before paint — a CDN/async font is an un-seeded input. Same rule as no-Date.now: every input is a pure function of the IR, now incl. which glyphs exist.

## 2026-06-22 — Render temp on the project disk, not the /tmp tmpfs (recurring 'disk full' fixed)
**Context:** Remotion writes a ~25MB webpack bundle + Chromium profile to os.tmpdir() = /tmp, which here is a 7.7G tmpfs (RAM). Hundreds of renders this session left 5.7G of orphaned remotion-webpack-bundle-* → tmpfs near-full → ENOSPC ('disk full') + crash. (The project itself is on /mnt/data with 69G free; the balloon was always /tmp.)
**Decision:** render.ts sets TMPDIR to PROJECT_ROOT/.render-tmp (on the big /mnt/data disk) before any bundle/Chromium temp, and rmSync's the bundle per render (finally). Verified: a render leaves /tmp flat. This supersedes the earlier swiftshader-balloon mitigations as the general fix (the GL backend was one cause; the tmpfs location was the other).

## 2026-06-22 — Nested composition (`clip`) = a pre-composition modeled on AE/Lottie precomp + Essential Graphics; per-instance namespacing is the determinism key (§13.3, DONE)
**Context:** M2 needs reusable nested timelines (§13.3 — the "brain-cell animation dropped into many videos"). The risk is inventing a bespoke nesting model; the discipline (CLAUDE.md r.3 "reuse over invent") is to ADOPT the editing-tool convention. Every tool we already align with has this exact feature: After Effects **pre-comp** / Lottie **precomposition** (a SHARED def + a `refId`), AE **Essential Graphics / Master Properties** / Premiere **.mogrt** (exposed params overridden per instance), and Remotion's native **`<Sequence>`** (a local timeline that resets the inner frame to 0). The job is only to WIRE the convention into our IR — not design a new one.
**Decision (what was built):**
- **Library (data):** `library/clips/<name>/<name>.clip.json` (catalog `kind: clip`, `uri: clip://<name>`): `{ params: { name:{type,default} }, duration_frames, layers:[ Scene-IR layer TEMPLATES ] }`. A template may reference an exposed param via `{ "$param": "name" }` (any value) or `"…{{name}}…"` (string interpolation), and may itself be a `clip` layer (nesting). Only `$param`-wired props are overridable — the Essential-Graphics rule. The loader gained `toClip(ref)` (parses via `ClipDefSchema`, mirroring `toStyleKit`/the `proc://` rig convention; keyed on the `clip://` URI scheme).
- **Story IR:** a `show[].clip` directive (mirrors `show[].shape/text`): `{ clip:<ref>, as:<handle>, at?:<anchor>, from?:<frame>, args?:{…params + group transform/z/parallax/effects} }`.
- **Scene IR:** `defs.clips[clipId] = { params, duration_frames, layers }` — the SHARED resolved def (the Lottie `assets` precomp), stored ONCE; and a `ClipLayer = { type:'clip', id, ref, z, transform?, effects?, parallax?, from?, duration_frames?, args? }` in the layer union. The def's `layers` are the recursive `LayerSchema` union (which includes `ClipLayer`) → arbitrary nesting; templates are loose records at the boundary (un-substituted `$param`/`{{}}` refs), validated post-substitution at render.
- **Lowering:** `collectClipDefs` resolves each `show[].clip` ref from the Library, puts its def in `defs.clips` ONCE (deduped by ref name), and RECURSES into a def's own `clip` layers resolving their refs too — with **cycle-detect** (a `visiting` path set throws on re-entry) + a **depth cap** (`MAX_CLIP_DEPTH=16`). `buildClipLayer` emits ONE `clip` LAYER per instance carrying `{ ref, args (non-structural params), transform (from `at`/scale/…), parallax, from, duration_frames, z, effects }`. The def is NOT expanded per instance — it stays shared (DRY, the Lottie/AE model).
- **Render:** `src/render/ClipLayer.tsx` renders `<Sequence from={from??0} durationInFrames={duration_frames??def.duration_frames}>` wrapping a transform/opacity GROUP (the whole unit moves together; inner parallax is `[0,0]` so it never double-shifts the outer camera/parallax) containing `def.layers` each run through the SAME `LayerView` dispatch (Scene.tsx — exported + reused). `src/render/clip.ts` holds the only genuinely-ours pieces: PURE `resolveParams(template, {...defaults, ...args})` (substitutes `$param`/`{{}}`), `mergeParamValues`, `namespaceId`, `deriveClipSeed`.
**Determinism (THE key risk — solved by per-instance namespacing):** the SAME clip used twice must not collide and must stay byte-identical. Every inner layer's effective id is namespaced `<clipLayerId>/<innerId>`, and any generator SEED derives from `deriveClipSeed(<namespaced id>)` (the same cyrb53 mixing the pipeline uses, re-implemented locally so render doesn't depend on the pipeline) — so the seed is a pure function of the per-instance id (distinct per instance, stable across runs), NEVER baked into the shared def. Resolution + render are pure (args + Sequence local frame). Verified per `verify-render` on CPU raster: `examples/clip-demo.yaml` places `lower-third` TWICE with different args (`Neurons`/`accent`/86 vs `Synapses`/`accent2`/42, the second `from:12`), and `lower-third` itself contains a `badge` clip containing a `scatter` generator (depth ≥ 2: lower-third → badge → {shape,text,generator}). All 5 stills byte-identical across two cold processes (`.cache`/`.remotion`/`node_modules/.cache` cleared between); the two instances render distinctly (different titles/accents/badge sparkle); `defs.clips` holds BOTH defs (deduped + recursed). typecheck clean; domain-clean + style-clean + delete-the-plugin all green; existing text/shapes demos unaffected.
**Lesson / pattern:** for any "reusable nested unit" feature, the reuse-over-invent move is to MAP each part onto an existing editing-tool concept (shared def = Lottie precomp / AE pre-comp; exposed params = Essential Graphics / .mogrt; local timeline = Remotion `<Sequence>`) and supply ONLY the IR wiring — no bespoke timeline/parameter machinery. The DRY split is "def stored once in `defs.clips`, instance is a ref + args" (exactly a Lottie file with one precomp used many times). The determinism key for any SHARED-def-with-instances feature is **per-instance NAMESPACING of ids + DERIVING seeds from the namespaced id** — never baking a seed into the shared def — so N instances are distinct yet each reproducible. The recursion (clip-in-a-clip) needs NO special machinery because the def's `layers` re-enter the SAME `LayerView` dispatch, which already includes the `clip` case. **Follow-on:** `environment` is just a clip used as a backdrop (a scene-template clip = a full background+ambience bundle) — same mechanism, no new entity.
**Files:** `src/ir/scene.ts` (`ClipLayer` + `ClipDef`/`ClipParam`/`Clips` schemas + `defs.clips`) · `src/ir/story.ts` (`show[].clip` + `from`) · `src/library/{catalog.ts (kind:clip + clips[]),loader.ts (toClip)}` · `src/pipeline/lower.ts` (`collectClipDefs` resolve+dedupe+recurse+cycle-detect, `buildClipLayer`) · new `src/render/ClipLayer.tsx` + `src/render/clip.ts` (resolveParams/namespaceId/deriveClipSeed), `src/render/Scene.tsx` (`clip` case + exported reusable `LayerView`) · new `library/clips/{lower-third,badge}/*.clip.json` + `library/index.json` (clip catalog entries) · new `examples/clip-demo.yaml` + `projects/clip-demo/`. Docs: clip design doc → DONE; spec §0 (Build status + NEXT-M2) + §15 (M2 list); CLAUDE.md (clip = nested pre-composition).

## 2026-06-22 — M2 vocabulary fast-track: the remaining editor vocabulary lands Tier-A-first, as PLUGINS/DATA, in one batched workflow (M2 DONE)
**Context:** After shape/effects/clip/text/shading, the ADR-003 backlog still had items 3 (transitions), 6 (color-script), 7 (asset vocab), 8 (compositing: attach/parts/environment + blend/matte), 10 (data-viz), plus a footage layer and alpha/codec output. Rather than per-feature workflows, we ran ONE workflow: a CORE lane (sequential, owns the shared core files) for the cross-cutting IR/render changes, CONCURRENT with disjoint PLUGIN lanes; ONE Integrate (wire `plugins/enabled.ts` + author `examples/m2-demo.yaml` exercising EVERY feature); ONE batched Verify (a single determinism + gate pass on the mega-demo), not per-feature.
**Decision (what was built, all Tier-A, never reimplementing a Remotion primitive):**
- **Compositional rigs (§8.1):** `attach` = inter-rig scene-graph parenting — a child layer rides a parent rig MOUNT. Mounts are rig-manifest DATA (`mounts[name].offset`), carried into the Scene-IR rig def by the loader; `src/render/attach.ts` is the genuinely-ours PURE compositor helper that composes the parent's evaluated `{a,k}` position + the named mount offset + an explicit `attach.offset` into a per-frame WORLD anchor, with `inherit` selecting which parent channels (position/rotation/scale/opacity) propagate. A rig is a typed BLACK BOX → core never pokes a provider skeleton (a position-anchor, not a full matrix). `parts` = an OPAQUE per-rig variant/skin map the provider interprets via its own spec (core passes it through). `environment` (§13.3) = a library clip used as a full-scene backdrop — a tiny lowering convention reusing the EXISTING clip machinery, NO new nesting entity (the clip follow-on promised in the prior entry).
- **Color-script (§11.4):** a beat carries `mood` (→ a named `library/palettes/*` token map) and/or an inline `palette`; lowering layers them over the resolved stylekit base (most-specific-last) and INTERPOLATES adjacent scene palettes in OKLab via `culori` (`src/pipeline/color-script.ts`) so a mood change reads as a smooth global shift. The per-scene palette rides Scene IR `scene.palette` and the renderer reads it through the existing `defs.stylekit` palette path → tokens flow into shading + fills. Pure (fn of palettes + t).
- **Footage + compositing:** a new generic `footage` LAYER plays external time-based media frame-SEEKED by Remotion — VIDEO via `<OffthreadVideo>` (frame-exact, off-thread, byte-deterministic) or LOTTIE via `@remotion/lottie` (JSON fetched once via `delayRender`/`staticFile`, no CDN). Compositing is a per-layer `blend` (CSS `mix-blend-mode` + `isolation`) + a track `matte`/`mask` (a sibling layer rendered into an SVG `<mask>` as a luma/alpha stencil, or an asset image as CSS `mask-image`) — applied GENERICALLY in the layer wrapper (`src/render/compositing.tsx`) on top of §11.1 shading + `effects[]` + parallax, for ANY layer type. Runtime's own primitives, never reimplemented; static styles → deterministic.
- **Transitions completion + the `core-transitions` PLUGIN:** the scene-boundary mapping moved OUT of core `Composition.tsx` (the hardcoded switch) INTO the engine's `transitions` extension point (previously an empty stub), populated by the new plugin via `api.registerTransition`. Core now resolves `transitions.get(scene.transition_in.kind).build(ctx)` (fade fallback only on a bare engine). DOM presets adopted from `@remotion/transitions` (fade/wipe/slide/iris); the kinds with no preset are small custom `TransitionPresentation`s on native primitives: **mask** (SVG radial-mask reveal), **morph-match/match-cut** (`flubber`-morphed shared-element over a crossfade), **camera-continuous** (one uninterrupted translate+scale across the boundary).
- **Plugin lanes (disjoint NEW dirs only):** **`core-dataviz`** registers a `chart` GENERATOR (bar/line/pie via `d3-shape`/`d3-scale` → SVG, animatable draw-on, own Zod params); **`core-objects`** registers a generic `object` PROVIDER (a peer of blob-creature: a small `ObjectSpec` → flat-vector SVG props, vendored, no network); **alpha/codec output** added to `src/cli/render.ts` — `--alpha` (transparent VP9/yuva420p WebM or ProRes 4444 or PNG-sequence), `--codec`/`--format` (mp4/webm/gif), default stays h264 mp4, CPU/GPU tier logic preserved.
**Verification (batched, per `verify-render`):** `tsc --noEmit` clean. All standing gates GREEN: domain-clean grep (EMPTY), style-clean grep (EMPTY), core-imports-no-plugin (EMPTY), delete-the-plugin (core typechecks EXIT 0 with all 5 new plugins moved aside). `examples/m2-demo.yaml` (a 4-beat story exercising environment + chart + attach + parts + footage + blend + matte + iris/mask/wipe + color-script warm→cold→hopeful) rendered on STILLS in TWO separate cold processes → all 5 PNGs byte-identical (md5 match); frames eyeballed correct (chart draws on, gear prop rides the rig mount, screen-blend glow + star-luma matte, palettes shift). Demo project written to `projects/m2-demo`.
**Lesson / pattern:** (1) The fast-track confirmed the plugin/data architecture SCALES — every new capability landed as a PLUGIN (dataviz/objects/transitions) or pure CORE machinery that stays generic (attach/color-script/compositing/footage), never a domain or style edit; the closing-the-`transitions`-stub is the canonical "move the hardcoded switch into the extension-point registry" refactor. (2) For "parent a layer to a rig," the reuse move is a POSITION-anchor from manifest mount DATA (not a live skeleton matrix) — keeps the rig a black box and the compose deterministic. (3) A SHARED-def feature (environment) needs NO new entity when an existing one (clip) already models it — resist inventing. (4) Batched verify (one mega-demo + one cold-process determinism pass) is the right granularity for a multi-feature milestone — far cheaper than per-feature renders and still proves the whole milestone wires together. **M2 (the ADR-003 Tier-A vocabulary) is COMPLETE;** remaining is M3 (audio/LLM/smart-layout/post-grade/AI-asset-gen), Tier-B GPU enablement, and small follow-ups (per-silhouette form-shade, far-layer atmospheric tint, Iconify bulk ingest, OTIO export, `factory bundle`/`list`).

## 2026-06-22 — Track-matte: asset (CSS mask-image) supported; layer (foreignObject) matte deferred
**Context:** The M2 batch shipped track-matte wired, but a LAYER (sibling) source via SVG `<mask>` + `<foreignObject>` does NOT gate in headless Chromium — verified empirically across 0×0 / full-size / luminance / alpha / xmlns variants: foreignObject HTML renders into a mask as EMPTY (would hide the content entirely).
**Decision:** (1) Matte SOURCE layers are now HIDDEN (consumed — the AE track-matte convention). (2) A NON-inverted ASSET matte (`matte.ref` → CSS `mask-image` of an SVG/PNG) is the reliable, SUPPORTED path. (3) A LAYER matte (`matte.from`) + inverted-asset matte DEGRADE GRACEFULLY (content shown UNMASKED + warn once), never silently hidden. (4) A proper layer matte (shape source → SVG `<clipPath>` from the source geometry) is the right reliable mechanism — a focused follow-up, deferred. Blend modes (mix-blend-mode) are unaffected + work.

## 2026-06-22 — Audio narration MVP: offline TTS → cached wav → audio[] → <Audio> → mux (M3 begins)
**Context:** First M3 capability. Story IR gives each beat a `say`; Scene IR reserved `audio[]` (AudioCueSchema). Need narration audio in the mp4 WITHOUT breaking determinism (golden rule 1) or the AI-never-touches-frames/runtime boundary (golden rule 2) — and the user chose Coqui XTTS, which is stochastic and heavy.
**Decision:** (1) **TTS is an OFFLINE build step**, the same posture as `factory:gen` / the vendored font — it runs at COMPILE time in `src/cli/render.ts` (gated `--no-audio`), never at render. The voice engine is wired behind a tiny swappable abstraction (`src/cli/narrate.ts`: `synth(text,voice,wpm)→wav`): **espeak-ng** is the deterministic, always-available DEFAULT; **Coqui XTTS v2** is wired via an isolated venv (`.venv-tts/bin/tts`) and FALLS BACK to espeak-ng with a warning if the binary is missing — we never fail the build on a missing optional engine. (2) **The cached wav is the deterministic artifact, NOT the engine.** Each wav is **content-addressed** (`hash(text+engine+voice+wpm)`, `src/cli/narrate-pass.ts` emits `kind:"narration"` cues at the beat's scene start, duration from ffprobe) and **skip-if-exists**, so the engine runs ONCE; a re-render replays the FIXED wav. This makes a stochastic TTS engine yield byte-deterministic output. (3) Playback is a plain Remotion `<Audio>` in a `<Sequence from={cue.at}>` (`src/render/Composition.tsx` `NarrationTrack`); `renderMedia` muxes audio automatically (h264+aac) — no mixing reimplemented (ADR-003 runtime-vs-compiler rule). Wavs are written DIRECTLY into the project's `assets/audio/` (= the render publicDir), so the bundle is self-contained with no extra vendor step.
**Rationale:** Generalizes the determinism rule from "no wall-clock in render" to "**offline-generated artifacts are the deterministic record**" — the right shape for ALL future stochastic/AI build steps (asset-gen, LLM script-expander): generate once into a content-addressed cache, render replays the fixed file. Keeps the voice engine fully swappable (the user can switch espeak-ng→Coqui→any TTS) without touching the render path.
**Verified (verify-render):** typecheck clean · `examples/narration-demo.yaml` → `projects/narration-demo` renders an mp4 with an aac audio stream starting at 0 · BOTH the decoded video stream (`ffmpeg -map 0:v -f md5`) AND the decoded audio stream (`-map 0:a -f md5`) byte-identical across two COLD processes · cache skip-if-exists confirmed (2nd compile: 0 synthesized / 3 cached) · `--no-audio` + existing silent demos unaffected (empty `audio[]`, silent track is Remotion's pre-existing h264 default at −91 dB) · domain-clean / style-clean / core-imports-no-plugin / delete-the-plugin all green · disk flat (`.render-tmp` 4K, wavs 220K).
**Note (Coqui needs an isolated 3.11 env):** Coqui XTTS pins old deps and an older Python; it MUST live in its own venv (`.venv-tts/`, Python 3.11, `COQUI_TOS_AGREED=1`), kept off the engine's critical path. `.venv-tts/` is gitignored. espeak-ng stays the CI/verification default precisely because it is deterministic, tiny, and dependency-free; Coqui is an opt-in quality upgrade whose output, once cached, is just another fixed wav.

## 2026-06-22 — Coqui XTTS env recipe (the working pin set, hard-won)
**Context:** User chose Coqui XTTS for narration. System Python is 3.14 (too new for torch/TTS); coqui-tts also doesn't auto-pull torch and is finicky about transformers/torchcodec. Five pins to get it running.
**Recipe (isolated, via uv — keeps the bleeding-edge system Python + GPU-less box from breaking it):**
1. `uv venv --python 3.11 .venv-tts` (system 3.14 has no torch/TTS wheels)
2. `uv pip install --python .venv-tts/bin/python coqui-tts`
3. `uv pip install --python .venv-tts/bin/python torch torchaudio --index-url https://download.pytorch.org/whl/cpu` (no CUDA on this box)
4. `uv pip install --python .venv-tts/bin/python "transformers>=4.57,<5"` (coqui wants ≥4.57; 5.x DROPPED `isin_mps_friendly` → import error)
5. `uv pip install --python .venv-tts/bin/python torchcodec` (torch ≥2.9 moved audio IO here)
Invoke: `COQUI_TOS_AGREED=1 .venv-tts/bin/tts --model_name tts_models/multilingual/multi-dataset/xtts_v2 --speaker_idx "Ana Florence" --language_idx en --text … --out_path …`. CPU RTF ≈ 3 (offline/cached, fine). `.venv-tts/` is gitignored; the ~2GB model caches in ~/.local/share/tts.
**Why it never risks the engine:** TTS is a build-time tool; the render imports no torch. espeak-ng stays the always-available default. The whole 5-error chain blocked nothing (espeak narration shipped meanwhile).

## 2026-06-23 — Remaining-backlog fast-track: captions / SFX / music+ducking / shape track-matte / generator-presets / bundle / list / OTIO
**Context:** Closing the M3-adjacent backlog after audio narration. Built CONCURRENTLY across one core lane (the shared render/pipeline files, sequential) + three disjoint lanes (presets / bundle+list / OTIO), then integrated. Every capability is Tier-A reuse or library DATA — no Remotion primitive reimplemented.
**Decision (what shipped):**
- **A1 Captions (§11.3/§12):** narration-synced on-screen subtitles, DERIVED from the SAME authored `say` text + cue window the narrate pass already has — deterministic WITHOUT whisper (we authored the line; golden rule 1). `SceneIR.captions[]` (parallel to `audio[]`), emitted by the narrate pass, rendered by `src/render/CaptionTrack.tsx` (bottom-centre pill, `<Sequence from=cue.at>`). Modes: `line` (whole line) / `words` (deterministic even-split cumulative reveal). Default-ON with narration; `--no-captions`. Dropped on an alpha render. We deliberately did NOT pull `@remotion/captions` (its value is page-grouping a whisper token stream — we don't have/need one).
- **A2 SFX-from-events (§12):** a sound effect attached to an EVENT — `show[].sfx` (element entrance → cue at scene start) or `beat.sfx[]` (a beat accent → cue at scene.at+offset). Synthesized OFFLINE by `ffmpeg lavfi` (sine/aevalsrc/anoisesrc + afade envelopes, FIXED seeds) into a SHARED content-stable `library/sfx/*.wav` cache (blip/pop/whoosh/ding/thud/click), copied into the project's `assets/audio/`, played via `<Audio>`. Same offline-cached-artifact posture as narration (golden rule 2).
- **A3 Music bed + ducking (§12):** a story-level `music` (a built-in synthesized bed — calm/drone/uplift, ffmpeg stacked-sine pads + tremolo, in `library/music/*.wav` — or an asset ref) plays UNDER the whole film via Remotion `<Loop>` + `<Audio>`, DUCKED while narration speaks. The duck is a PURE per-frame `volume(frame)` (lerp gain→duck with linear fade ramps around each narration window, MAX across overlapping windows) computed in `Composition.tsx` — deterministic, needs no audio analysis. The `<Loop>` global-frame is recovered via `Loop.useLoop()` (the per-iteration frame the `<Audio volume>` callback sees is NOT the global timeline frame — important gotcha).
- **A4 Track-matte (the deferred 2026-06-22 follow-up, now DONE — with a determinism course-correction):** a LAYER matte whose SOURCE is a SHAPE builds the source shape's screen-space geometry (`shapeClipGeometry` in `ShapeLayer.tsx`, mirroring `<ShapeLayer>`'s placement exactly: translate to box top-left, rotate/scale about the box centre) and renders it as a composition-sized SVG stencil (white path on transparent) applied as a STATIC CSS `mask-image` **data-URI** (`maskMode:luminance`, `isolation:isolate`) — `src/render/compositing.tsx`. This CLIPS reliably in headless Chromium (unlike the foreignObject `<mask>` that renders empty) AND — the load-bearing point — is **BYTE-IDENTICAL across cold `renderMedia` processes**. COURSE-CORRECTION: an earlier iteration of this fast-track used a LIVE SVG `clip-path: url(#…)`; it was byte-identical under `renderStill` but DRIFTED ~51 dB along the ~1px anti-aliased clip edge across cold continuous-video processes (the vector clip re-rasterizes its AA edge per frame with sub-pixel drift, independent of GL backend/concurrency). We REPLACED it with the data-URI `mask-image` (the proven byte-identical asset-matte path); the shape track-matte is now byte-deterministic in the muxed video with NO perceptual-only carve-out. Non-shape sources + inverted shape mattes keep the graceful warn. Matte-source layers stay hidden (AE convention).
- **B Generator presets (ADR-004 §2):** a `generator-preset` is library DATA — a `{ gen, params }` sidecar (`library/generators/<name>.preset.json`) + a `generators` catalog entry (`preset://<name>`). `Library.expandGeneratorRef(genRef, layerArgs)` expands a preset ref → its locked `{ gen, params }` (preset params = defaults, the layer's args win); a bare impl name passes through. WIRED into lowering (`buildGeneratorLayer` calls `expandGeneratorRef`; the id/seed key on the AUTHORED ref so they stay stable). New CLI `factory:gen-preset <spec.json>`. Shipped two presets: `starfield`, `dust-motes` (both → `scatter`). The generator CODE is unchanged — a preset is pure DATA.
- **C bundle + list:** `factory:bundle <id>` packages a project into a self-contained `dist/bundles/<id>.afbundle/` (scene.json + lock + project.json + all vendored assets incl. audio wavs + media + a content-hashed `bundle.json`; optional `--zip`) — the `.otiod`/dotLottie role. `factory:list [library|projects] [--kind N] [--json]` inventories the catalog (by namespace) + compiled projects. Both read-only, no library re-resolution.
- **D OTIO export:** `export:otio <id>` → a valid OpenTimelineIO `.otio` (Timeline.1/Stack.1/Track.1/Clip.2/Gap.1/Transition.1/…) emitted BY HAND (no Python `opentimelineio` dep). Scenes → a flat editorial "Scenes" video track (clips + gaps + transitions, `transition_in.kind` → SMPTE_Dissolve / Custom_Wipe); `audio[]` → an "Audio" track of ExternalReferences; the full per-scene layer graph rides losslessly in namespaced `metadata["animation-factory"]`. Pure fn of scene.json.
- **Integration:** `export:otio` added to `package.json` (the other three scripts pre-existed); `expandGeneratorRef` added to lower's `LibraryLike` facade + threaded through `buildScene`→`buildGeneratorLayer`; the m2-demo matte panel recolored to a high-contrast `#ff4d6d` so the star clip reads. No `plugins/enabled.ts` change needed (the new CLIs are not plugins; the presets are library data; the audio passes are compile-path wiring already imported by `render.ts`).
**Mega-demo:** `examples/remaining-demo.yaml` — a 3-beat narrated story (warm→cold→hopeful) exercising narration + captions + a `pop` element-entrance SFX + a `whoosh`/`ding` beat accent + a `calm` music bed (ducked) + a STAR-clipped track-matte panel (bright `#ff4d6d`) + the `starfield` generator PRESET (args override the preset count). Builds with `tsx src/cli/render.ts examples/remaining-demo.yaml --project remaining-demo --name "Remaining"`. All four CLIs verified on it (list shows the presets + project; bundle = 17 files content-hashed; otio = 3 scenes/11 layers/7 audio cues).
**Verification (batched, per verify-render):** `tsc --noEmit` clean. domain-clean / style-clean greps clean (only doc/comment + DATA-name refs). delete-the-plugin test green (move `plugins/blob-creature` aside → `src/` still typechecks, no `plugins/` import). Stills (`--frames auto`) eyeballed: starfield preset + caption pill (b1), hero entrance (b2), and the bright `#ff4d6d` panel cleanly STAR-CLIPPED with the matte-source star hidden (b3). **Determinism — BYTE-IDENTICAL cross-process (the data-URI mask fix delivered):** two COLD full `renderMedia` processes of `remaining-demo` produced the SAME decoded video stream md5 (`cc6587…f30f3`, `ffmpeg -map 0:v -f md5`) AND the SAME decoded audio stream md5 (`262e12…fd9f`, `-map 0:a -f md5`) — INCLUDING the matte beat (the static `mask-image` data-URI composites as an image layer, not a per-frame-rasterized vector clip). **Audio AUDIBLE:** `volumedetect` mean −23.2 dB / max −5.2 dB (narration + sfx + ducked bed), far above the −60 dB silent floor; the aac track is present.
**Lesson / standing rule UPDATE:** (1) **AUDIO-must-be-audible is a verify gate** — a build with audio is checked BOTH for cross-process byte-identity (decoded video md5 AND decoded audio md5) AND for loudness (`volumedetect` mean ≫ −60 dB); a present-but-silent track is a regression that md5-only checks miss. (2) **The matte clipPath fix supersedes the deferred no-op** — a SHAPE-source track-matte is now real (geometry stencil) AND byte-deterministic, achieved by composing the stencil as a STATIC `mask-image` data-URI rather than a LIVE SVG `clip-path` (a live vector clip drifts ~51 dB on its AA edge across cold `renderMedia` processes — byte-exact under `renderStill` only). Standing nuance preserved: when you DO need a live per-frame vector clip/blur, that edge is a perceptual-only zone (verify with SSIM/VMAF); the byte-exact path is the static-image mask. **Remaining after this:** lip-sync, whisper word-alignment (precision captions), LLM script-expander, smart layout/camera director, Tier-B GPU, formal AssetProvider/LibraryResolver interfaces.

## 2026-06-22 — Pipeline cache invalidation: bump PASS_VERSION (or clear .cache) after a pass code change
**Context:** During the footage `muted` fix, editing `src/pipeline/lower.ts` had no effect — the rendered `scene.json` kept the old shape. Root cause: the pipeline content-hash cache key = the script contents + each pass's `PASS_VERSION` (+ `cacheKeyExtra`), **NOT the pass's source code** (nor library state). So changing a pass's logic without bumping its `PASS_VERSION` returns a STALE cached Scene IR; the code change silently doesn't take.
**Decision:** After editing a pass's CODE (`lower`/`layout`/`camera`/`colorscript`/`narrate`/…), **bump that pass's `PASS_VERSION`** (the correct, durable fix — invalidates exactly the affected entries) **or** `rm -rf .cache` (the quick dev fix). When *verifying* a pass change, do this FIRST or you verify stale output. Same applies after editing `library/*` (the key omits library state). Folded into CLAUDE.md golden rule 4 + the verify-render skill's stale-pipeline-cache gotcha.
**Why not auto-hash the source:** hashing every pass's transitive source into the key is brittle/slow; explicit `PASS_VERSION` (already folded into the key for per-stage versioning) is the intended lever — we just have to remember to bump it.

## 2026-06-23 — Painting style = stylekit `paint` DATA + a generic shading mechanism (ADR-009)
**Context:** Objects could be placed and §11.1-shaded (supporting drop-shadows + a scene light wash) but shapes still read as FLAT vector fills; the reference (Kurzgesagt nature, `style-ref/pHJIhxZEoxg`) is *painted* — volumetric form-shading, palette shade-ramps, glow, a dark atmospheric backdrop + vignette + focal pool, faint in-fill texture. Question: is "painting" a per-object plugin or a TRANSFORM over content?
**Decision:** Painting is a **transform over content, not a set of objects** (design `2026-06-23-painting-style-system-design.md`, ADR-009): an OPTIONAL stylekit **`paint` DATA** sub-table (schema in `src/ir/stylekit.ts`; VALUES in `library/stylekits/*.json`; absent → flat) + a generic MECHANISM (`src/render/paint.ts` pure helpers, wired in `shading.tsx`/`Scene.tsx`/`ShapeLayer.tsx`, gated by `floor.shading`) applied to EVERY shape/asset layer:
- **Form-fill** — a solid `fill` token → an SVG linear/radial gradient whose stops are an **AUTO `culori` shade-ramp** (OKLab L±`shadowL`/`highlightL` + warm-highlight/cool-shadow hue shift) along `lightDeg`; any content paints volumetric with ZERO per-token authoring.
- **Rim** — a lighter inner stroke (`rimColor` = the ramp highlight) masked by a light-direction gradient so the bright edge sits on the lit side only (wired in `ShapeLayer` as a second masked `<path>`).
- **Texture + Glow** — emitted as the **core-effects `grain`/`glow`** ops (seeded from the layer id) through the SAME engine `effects` registry the authored `effects[]` use — paint never reimplements them.
- **Atmosphere (scene-level)** — `<Atmosphere>` dark backdrop gradient + warm focal pool behind the world, the `<SceneLook>` vignette scaled by `paint.atmosphere.vignette`, and a per-layer **depth grade** (`depthGrade`) darkening/desaturating far (low-parallax) layers toward the atmosphere.
The `kurzgesagt-nature` stylekit DATA tunes these toward the reference (deep purple/green/orange palette + a warm light, dark backdrop, glow, shade deltas, grain); registered in `library/index.json`.
**Decisions locked (as recommended):** AUTO culori ramps (a fill is a colour family, not authored per token) · grain texture IN the MVP · forest-cross-section study target.
**Rationale:** build once → every object looks painted; content stays dumb, the STYLE carries the look (ADR-006/007 — core/front-end specialize in nothing; a per-object painting plugin would re-couple style to content). Reuse over invent: culori for all colour math, core-effects glow/grain, extend `SceneLook`/`light`/`shading` (no Remotion primitive reimplemented).
**Verification (per verify-render):** `tsc --noEmit` clean. **Determinism — BYTE-IDENTICAL across two cold processes** on the study frames (culori + SVG gradients + seeded `feTurbulence` are pure; CPU raster). **Paint visibly applied:** the forest study (`examples/forest-study.yaml`, deliberately dumb ellipses/rects + a scatter field) renders form-shaded green canopy blobs with a lit rim, a warm focal glow behind dark trunks, a dark purple atmospheric backdrop + vignette, grain, and scattered flower dots — clearly painted. **Opt-out proven:** the SAME shapes under `plain` (`floor.shading` off) render FLAT (no paint). **Gates green:** domain-clean + style-clean (incl. the new nature hexes — values live in DATA) + delete-the-plugin + no `plugins/` import in `src/`. **No regression:** m2-demo (default `kurzgesagt`, no `paint`) renders unchanged.
**Lesson / standing rule:** STYLE is a transform over content (a stylekit `paint` DATA model + a generic shading mechanism applied to all shapes), NOT a per-object capability — folded into CLAUDE.md golden rule 7 + spec §11.1. The `paint` sub-table is OPTIONAL and floor-gated so it is purely additive (default look unchanged). **Remaining to push closer to the reference (all DATA/mechanism, no architecture change):** richer in-fill texture (stipple/canvas grain, not just noise); a stronger rim that follows curvature; per-token ramp overrides for the deep trunk tone (a near-black fill barely ramps); and an OPT-IN for generators to ramp their own internal fills (scatter/foliage) so procedural content matches the shape layers.

## 2026-06-23 — Library-breadth + finishing-passes batch: M7a interfaces / M7b icons / M7c maps / M8a post grade / M8b paint follow-ons
**Context:** Closing the post-paint "cover all of it" roadmap Batch A/B items that are pure CPU/SVG (byte-identical, no installs): the last open ADR-001 interface work, library vocabulary breadth (icons + maps), and the two finishing passes (`post[]` grade + the listed paint follow-ons). All ran AFTER the painting-style workflow landed (it owned the render core + `library/index.json`), lane-separated so they did not collide.
**Decision (what shipped, all reuse/DATA — no Remotion primitive reimplemented, no core domain edit):**
- **M7a — ADR-001 formal interfaces (`src/library/interfaces.ts`).** The two boundary seams ADR-001 reserved are now explicit TS interfaces: **`LibraryResolver`** (§4 storage seam — catalog `name@version`→content hash, dedup, lockfile pin/verify) + **`AssetRefResolver`** (§2/§5 library-side ref→def adapter — `toRigDef`/`toAssetDef`/`toStyleKit`/`toClip`/`toPalette`/`toGeneratorPreset`/`expandGeneratorRef`, keyed on the URI SCHEME so core names no provider/look/clip). `Library implements LibraryResolver, AssetRefResolver` is a COMPILE-TIME ASSERTION that the existing loader methods satisfy both verbatim — **ZERO behavior change** (descriptive, not prescriptive; the seams are now explicit so a remote registry / alternate ref-adapter is a drop-in, not a refactor). Naming nuance: the render-time `AssetProvider` half (`instantiate`/`render`/`dispose`) lives in the PROVIDER PLUGINS (`providers.get(id)`) per ADR-005/006/007, not core — so `src/library` owns only the library half, named precisely `AssetRefResolver`. ADR-001 status → Integrated/DONE; its follow-up item struck through.
- **M7b — icon library (`factory:ingest-icons`, `src/cli/ingest-icons.ts`).** Ingest open-license SVGs (lucide-static, ISC) ONCE OFFLINE → parse out path `d` + viewBox (GEOMETRY ONLY, never raw markup) → core-objects `ObjectSpec` (`kind:"icon"`) → canonical sidecar `library/icons/<id>/<id>.spec.json` → content-hash → register/update the `icons` catalog namespace with provenance + license. Renders as a form-shadeable colored BADGE + glyph outline, so the active stylekit shades it for free (no per-icon styling). The offline fetch is the build step; the render replays the FIXED cached spec → byte-deterministic (golden rule 1/2, the TTS pattern). ADR-007 code-location: the core-objects `ObjectSpecSchema` validator is INJECTED by a repo-root entry (`ingest-icons-entry.ts`, like `render-entry.tsx`/`factory:gen`), so `src/` imports no plugin (delete-the-plugin gate holds). Shipped ~10 icons (heart/leaf/star/globe/sun/cloud/rocket/lightbulb/tree_pine/sprout).
- **M7c — `map` generator (core-dataviz, `plugins/core-dataviz/map.tsx` + `types.ts` `MapParamsSchema`).** A peer of `chart` in the SAME plugin, registered via `api.registerGenerator('map', MapGen)`. Adopts **d3-geo** (projection set + `geoPath` SVG path generator) + **topojson-client** (`feature` to decode TopoJSON→GeoJSON) — projection/topology math NOT reimplemented (golden rule 3). The GEOMETRY is DATA (inline TopoJSON `topology`+`object` OR inline GeoJSON in `params`, exactly like `chart` carries `data`), selected by naming a generator-preset (`library/generators/world-map.preset.json` → `library/maps/world-110m.json`, with a LICENSE sidecar) — so the plugin logic is DOMAIN-CLEAN (knows how to project+draw arbitrary GeoJSON; hardcodes no country/region). Features: selectable projection (NaturalEarth1/Mercator/EqualEarth/Equirectangular/Orthographic/AzimuthalEqualArea), auto-`fit` to the layer box, optional choropleth (`{key:color}` keyed by a feature `key_field`), frame-driven draw-on (stroke-dashoffset reveal + fill fade reusing the shared `scale.ts` ramp). PURE fn of (params, frame) — `geoPath` emits identical path strings, no RNG/clock (`seed` accepted but unused) → byte-deterministic.
- **M8a — `post[]` composition grade (`Composition.tsx` `PostGrade`).** The reserved Story-IR `post` (validated by the Scene-IR `PostSchema`, carried verbatim by lowering into Scene-IR `post[]`) is now WIRED to a FILM-LEVEL effect stack wrapping the WHOLE composited frame (the entire `<TransitionSeries>`), applied AFTER the scene graph. It REUSES the exact core-effects ops + compositor glue the per-layer `effects[]` use (`resolveEffects`/`applyEffects`, which look each `kind` up in the engine `effects` registry) — NO new effect engine. A fixed scene-independent layer id (`__post__`) → a stable SVG `<filter>` id → byte-identical markup. Pure fn of (`post[]`, frame); empty/absent → `applyEffects` returns the child untouched (STRICT no-op, byte-identical to before). DROPPED on `--alpha` (grading flattens the alpha channel + belongs to the finished film, not a compositing plate); wraps only the visual frame, never the audio/caption sibling tracks.
- **M8b — painting-style follow-ons (`src/render/paint.ts`, all DATA/mechanism — no architecture change).** The ADR-009 listed follow-ups: (1) a SOFTER 4-stop form-shade ramp (highlight→base→mid→shadow — a midtone reads less plasticky); (2) PER-SILHOUETTE ramp orientation — round/blobby shapes bias to a RADIAL volume (read as spheres), elongated shapes (aspect far from 1) keep the kit's LINEAR ramp along their OWN major axis (read as cylinders), hard low-blobiness shapes keep the kit type; (3) AERIAL PERSPECTIVE — far (low-parallax) layers haze their WHOLE shade-ramp fill TOWARD the atmosphere mid-tone (`mixToward`/`atmosphereTintColor`), which is SILHOUETTE-PERFECT (it tints the fill colour itself, not a rectangular overlay), so the outer depth grade only needs a gentle extra desaturate (a heavy brightness cut on top of the haze crushed dark trunk tokens to near-black — trimmed). The `kurzgesagt-nature` stylekit DATA was retuned for the softer ramp + haze.
**Verification (per verify-render):** `tsc --noEmit` clean (EXIT 0). All standing gates green: domain-clean grep + style-clean grep (icon/map/post are DATA-name + doc refs) + core-imports-no-plugin (`grep "from .*plugins/" src/` empty — the icon CLI takes the validator as a parameter) + delete-the-plugin (move a plugin dir aside → `src/` still typechecks). Demo scripts `examples/{icon,map,post}-demo.yaml` → `projects/{icon,map,post}-demo`. (No item FAILED.)
**Lesson / standing rules:** (1) **A boundary contract can be formalized with ZERO behavior change** — a descriptive `implements` clause on the existing impl turns an architectural intent (ADR-001's reserved seams) into a compile-time-enforced contract; the precise split (`LibraryResolver`/`AssetRefResolver` in core; `AssetProvider.instantiate/render/dispose` in the plugins) keeps core plugin-free. (2) **A new generator that consumes external geometry is a peer in an existing plugin + a generator-preset for the DATA** — `map` reused the `chart` pattern (logic in the plugin, geometry as preset DATA) so it stayed domain-clean and needed no new extension point. (3) **A bulk-vocabulary library (icons) is the cheapest reuse** — fetch-once-offline → content-addressed spec → render replays fixed bytes (the TTS/asset pattern), and the provider-schema validator is INJECTED from the composition-root entry so the ingest CLI in `src/` imports no plugin. (4) **`post[]` proved the per-layer effects engine generalizes to the whole frame for free** — wrapping the `<TransitionSeries>` as "the layer" and reusing `resolveEffects`/`applyEffects` meant no new effect code, and the empty-array strict-no-op keeps every prior render byte-identical. (5) **Painting fidelity is pure DATA/mechanism iteration** — the silhouette-perfect way to haze a far object is to tint its FILL ramp (not overlay a rectangle), and value shifts must be done ONCE (in the ramp) not stacked (ramp haze + outer grade) or dark tokens crush. **Remaining after this batch:** lip-sync, whisper word-alignment captions, LLM script-expander, smart layout/camera director, Tier-B GPU (perceptual tier), AI asset-gen; plus the ADR-001 `LibraryResolver` REMOTE variant + a specified bundle/export format.

## 2026-06-23 — Remaining roadmap COMPLETE: M4a/M4b/M5/M6/M9 (whisper captions · lip-sync · director · GPU · AI asset-gen)
**Context:** The whole "cover all of it" tail (`docs/factory/REMAINING-ROADMAP.md`) — precision audio, the director, the GPU tier, and AI asset-gen — landed in Batches B/C with the toolchains installed in isolated venvs at repo root. Each had to satisfy a golden-rule determinism strategy DIFFERENT per item.
**Decision (one per item, all shipped):**
- **M4a — whisper word-sync captions (`src/cli/narrate.ts`).** faster-whisper in `.venv-whisper` (model "small", `HF_HOME` pinned) + `scripts/tts/align_whisper.py` force-aligns each cached narration wav to its transcript OFFLINE → per-word seconds. `alignNarration()` caches the alignment content-addressed (`assets/audio/align/<hash>.json`; `alignHash` = wav-hash + transcript + model; skip-if-exists); `timedWordsFromAlignment()` maps seconds → CaptionCue `wordsTimed[]` (local frame `at`/`dur`); `CaptionTrack.tsx` reveals words on their REAL spoken times in `words` mode. A cache hit replays the FIXED JSON → byte-deterministic though whisper isn't bit-exact across machines. Missing venv/model/alignment-error → the even-split `words[]` fallback (never fails the build); `--no-word-align` skips the slow first-run whisper step.
- **M4b — lip-sync visemes via a GENERIC mouth channel (`narrate.ts:mouthTrackForNarration`).** A per-frame mouth-OPENNESS track derived OFFLINE from the cached wav (ffmpeg `f32le` decode → windowed RMS energy envelope → 0..1 with a gamma lift + an attack/decay smoother so the mouth opens fast/closes slower, no chatter), CACHED content-addressed (`mouthHash` = wav-hash + fps + frames + analyzer ver); when a whisper alignment exists, coarse per-frame viseme LABELS (in-word vs gap) come for free (no extra whisper run). The narrate pass attaches it to the SPEAKER rig layer's generic OPAQUE `mouth` channel (`MouthTrackSchema` in `scene.ts` = `open[]` + optional `viseme[]`). CORE NEVER INTERPRETS A SAMPLE — it is just an array the PROVIDER reads (the blob-creature provider drives its mouth part from it); other providers ignore it. Pure fn of (track, frame) → deterministic; opt-in (`--no-lip-sync`); no narration → no track (rig idles).
- **M5 — director: heuristic NOW + a clean `claude -p` LLM seam (`src/pipeline/director.ts`).** A `Director` interface (`plan(ir) → DirectorPlan`, NEVER mutates) + a pure `applyPlan` fold that runs BEFORE the lite layout (P6) + camera (P8) passes; AUTHOR ALWAYS WINS (it only fills FREE placements + an UNSET camera intent). DirectorPlan coords are FRACTIONAL (0..1 of the frame → aspect-independent + LLM-reasonable). Two impls: **HeuristicDirector** (DEFAULT, pure/local/free — scores rule-of-thirds candidate slots clamped to an aspect-scaled safe area by focal weight + headroom (text up / grounded down) + balance (alternate sides), and picks a camera preset NAME by structural beat intent: first scene = establishing, 1 focal = push-in, ≥3 = pull-out, else hold — the MECHANISM here, the move RECIPES stay DATA in `library/camera/presets.json`); **LlmDirector** (opt-in `kind:"llm"` — shells to `claude -p --output-format json` keyless + no-tools + `--strict-mcp-config` ONCE with a tight FRACTIONAL-coords prompt, validates the output against `DirectorPlanSchema` + the LIVE preset table, caches content-addressed under `.cache/director/plan-<key>.json`; a cache hit replays the FIXED plan → render is byte-deterministic + fully offline though the LLM isn't — exactly the TTS pattern; ANY failure (no binary / non-zero exit / invalid output) → HeuristicDirector fallback, `DIRECTOR_DEBUG=1` surfaces why). This same seam is the spine of the future script→IR expander.
- **M6 — Tier-B GPU effects, the ONE perceptual (non-byte-exact) tier (`plugins/gpu-effects`).** A plugin contributing WebGL ops into the generic `effects` + `transitions` registries by ADOPTING `pixi-filters` (CRT/AdvancedBloom/Shockwave/Godray/Glitch) on `pixi.js` v8 + a `gl-transition` dissolve (NO hand-written shaders — reuse over invent). DOUBLE-GATED so the CPU raster default stays byte-identical: a LOAD gate (registered ONLY when `render.ts --gpu` sets `process.env.GPU_TIER` via a webpack `DefinePlugin`) + a RUNTIME self-gate (`PixiHost.gpuActive()` requires a REAL hardware WebGL context; software-WebGL rejected). Each draw is a PURE fn of `frame`; the GPU is the only non-determinism, accepted by the tier — verified PERCEPTUALLY with VMAF (`ffmpeg libvmaf`) against a reference, NEVER `cmp`. The CPU tier never loads this plugin → byte-exact.
- **M9 — AI asset-gen: OpenVINO SD, cached (`src/cli/imagegen.ts`, `factory:imagegen`).** Content-address {prompt, seed, model, steps, size, negative, guidance} → reuse a cached PNG OR run `.venv-sd/bin/python scripts/imagegen/sd_openvino.py` (OpenVINO SD on the Iris Xe iGPU, `HF_HOME` pinned) ONCE into the cache → copy to `public/generated/` + `library/generated/` → register an `asset` catalog entry (`kind:asset`, `format:image`, `uri:asset://generated/<id>.png`) with provenance + model license. AI touches ONLY the offline library (golden rule 2); the render replays the FIXED PNG (no provider needed — the AssetLayer draws an `image` asset directly → core stays plugin-free). Build-time only → render stays fast + byte-deterministic though SD isn't bit-exact (cached PNG is the record, like TTS/whisper).
**Rationale:** Each is determinism-via-a-cached-offline-artifact (TTS pattern) EXCEPT the heuristic director (pure CPU) and Tier-B GPU (perceptual VMAF tier). No core entity was added: the mouth channel is OPAQUE (provider-interpreted), the director is a pipeline pass folding into existing IR, the GPU ops are a plugin, and a generated image is just an `asset` (no provider).
**HONEST failures / env-gated (none faked):** the pre-exported OpenVINO SD models FAILED to load on this box (a transformers `CLIPFeatureExtractor` mismatch). imagegen handles this per golden rule 1 by synthesizing a DETERMINISTIC placeholder PNG and recording "placeholder (.venv-sd unavailable)" in the asset provenance — the build NEVER fails — so the M9 MECHANISM is complete and verified, but REAL generations remain gated on pinning transformers in `.venv-sd` (or re-exporting a base/turbo SD with `optimum export=True`). Likewise `gl:"angle"` (the iGPU) is the only non-byte-exact path and is gated OFF by default. No item is faked as passing.
**Lessons / standing rules:** (1) **A provider-specific signal rides a GENERIC opaque channel, not a new core field** — lip-sync is a `mouth: {open[], viseme[]}` array the core blindly carries and the provider interprets, so other providers ignore it and "character mouth" never leaks into the engine. (2) **The LLM is made deterministic exactly like TTS** — shell to `claude -p` ONCE, validate + content-address-cache the EMITTED DATA, replay forever; an unavailable/bad LLM falls back to a pure local impl so the build never depends on the network. (3) **Whisper/alignment is the TTS pattern again** — run ONCE offline into a content-addressed cache, replay the FIXED JSON → byte-identical though the engine isn't bit-exact; ALWAYS ship the even-split fallback so a missing venv never fails the build. (4) **The non-byte-exact GPU tier must be DOUBLE-GATED** (build-time DefinePlugin LOAD gate + runtime real-WebGL self-gate) and verified with VMAF, so the byte-exact CPU default literally cannot load it. (5) **When an installed toolchain genuinely cannot run, ship the MECHANISM + a deterministic placeholder and REPORT it honestly** — never fake a generation; the cache makes the placeholder swappable for the real artifact the moment the env is fixed.
**Remaining after this batch (deferred, NOT roadmap tail):** the ADR-001 `LibraryResolver` REMOTE variant · a specified bundle/export format · real generated SD assets (pending the `.venv-sd` model fix) · whisper-PRECISION caption page-grouping · a full script→IR LLM front-end (the M5 LLM seam is its spine).

## 2026-06-23 — Render concurrency is RAM-aware (fixes the headless-Chrome OOM crash)

**Symptom:** full-video renders intermittently failed with Remotion "Target closed" / "browser crashed
while rendering frame N, retrying" — most often on blur/alpha-heavy scenes. NOT a determinism or disk
issue (that was the gl→CPU-raster decision); a stability one.

**Cause:** `CONCURRENCY = cpu-2` (this box: 16 cores → 14 headless-Chrome render workers), but only ~3-4 GB
RAM free. ~14 Chrome renderers × ~1 GB each ⇒ OOM ⇒ Chrome crashes mid-render. The setting was RAM-blind.

**Fix (`src/cli/render.ts`):** cap the default concurrency by BOTH cores and FREE MEMORY —
`min(cpu-2, floor(freemem / 1.3 GB))`, min 1 — so workers fit available RAM (this box: 14 → 2).
`RENDER_CONCURRENCY=N` still overrides explicitly. Note: only full-video `renderMedia` uses concurrency;
`--frames auto` stills are single-frame (`renderStill`) and were never the crash path. `/dev/shm` is 7.7 G
here, so `--disable-dev-shm-usage` was NOT the issue. Lower concurrency also reduces RAM thrashing, so
renders are faster + more reliable on a constrained box.
